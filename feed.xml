<?xml version="1.0"?>
<rss version="2.0">
  <channel>
    <title>sukawasatoru.com</title>
    <link>https://sukawasatoru.com</link>
    <description>個人用のメモと流し読みする文章を書きます</description>
    <lastBuildDate>Sun Apr 25 2021 15:04:15 GMT+0000 (Coordinated Universal Time)</lastBuildDate>
    <item>
      <title>2021年 4月時点で使っている View の構成</title>
      <link>https://sukawasatoru.com/docs/2021-04-26-android-mvp-mvvm</link>
      <guid>https://sukawasatoru.com/docs/2021-04-26-android-mvp-mvvm</guid>
      <description><![CDATA[<h1>2021年 4月時点で使っている View の構成</h1><p>おさらいとして <a href="https://github.com/android/architecture-samples">Android Architecture Blueprints v2</a> を確認しましょう。</p><p><em>android / architecture-samples</em> の XXX-mvp は</p><ul><li>Activity が Fragment や Presenter の setup をしている</li><li>Fragment が TasksContract.View を implements しており View へ命令を出している / View の Event を受け取り Presenter へ命令している</li></ul><p>ということをしている。それに加え</p><p>todo-mvp{,kotlin} は</p><ul><li>Presenter が TasksContract.Presenter を implements しており Fragment や Repository へ命令している / Repository の結果 (callback) を受け取り Fragment へ命令している</li></ul><p>todo-mvp-clean は</p><ul><li>Presenter が TasksContract.Presenter を implements しており Fragment や Task へ命令している / Task の 結果 (Callback) を受け取り Presenter へ命令している<ul><li>Task への命令は UseCaseHandler を使用している / Dispatchers.Default のように裏で Task を実行して結果を Dispatchers.UI で返すというようなことを (テストのために) 抽象化している</li></ul></li><li>UseCase は特定の入力により結果を返すという関数のようなもの / Repository に依存しているので必ず冪等性があるわけではない / XX をする、 YY をする、といった単位で使われる<ul><li>Future みたいなやつ (正確ではないけれど大体そんな感じ)</li><li>com.example.android.architecture.blueprints.todoapp.UseCase はそれを表現したもの</li></ul></li><li>Task が UseCase を extends しており Repository へ命令している</li></ul><p>といった感じです。</p><p>私が個人の時によく使う構成は</p><ul><li>TasksContract に相当するものは使っていない<ul><li>View に関連する JUnit を書かなくなったので省略するようになった<ul><li>できるだけ冪等性のある関数を実装するようにしてテストしている</li></ul></li></ul></li><li>Activity が Fragment を setup する</li><li>Fragment は View (ViewGroup) を生成するだけ</li><li>View (ViewGroup) が Presenter 相当の命令を View 対して行う<ul><li><code>flow {}.asLiveData().observe()</code> する</li><li><code>flow {}</code> / <code>callbackFlow {}</code> 系が増えてきたら機能ごとに LifecycleObserver を implements した class に切り出し</li><li>onCreate 以外に onStart や onResume を扱いたくなった時は LifecycleObserver を implements した class に切り出し</li></ul></li><li>ViewModel を扱うときは <code>Flow.cachedIn()</code> や MutableSharedFlow といったデータの入れ物として使用して MyViewModel.start() のような Method はほぼ実装していない<ul><li>ほとんどないと思うけど XXX を持たせちゃって Memory leak していた、というのを考えるのが面倒</li><li>今後 Fragment ちゃんとやるとしたら Presenter は Fragment が対応して Task は ViewModel が対応するようになるんだろうなーとは思いつつ <a href="https://developer.android.com/topic/libraries/architecture/viewmodel">(Guide)</a></li></ul></li></ul><hr/><p>timestamp<br/>
<!-- -->2021-04-26 (First edition)</p>]]></description>
      <pubDate>Mon Apr 26 2021 00:00:00 GMT+0000 (Coordinated Universal Time)</pubDate>
    </item>
    <item>
      <title>Android の Fragment が分からない</title>
      <link>https://sukawasatoru.com/docs/2021-04-25-android-fragment</link>
      <guid>https://sukawasatoru.com/docs/2021-04-25-android-fragment</guid>
      <description><![CDATA[<h1>Android の Fragment が分からない</h1><p>Fragment の機能は一通り使ってみて、 onAttach ではこうして onCreate ではこれをして onCreateView ではこれをして etc. というのをしばらくの間やっていました。</p><p>数年前 (Kotlin Coroutine が普及する前で Agera とか RxJava いいよねとなっているあたり) ふと思ったのが Fragment を使ったことで何かいいことがあったか、というもので、ちょっと考えてみてもドキュメントの通りに実装したことに満足したけど画面遷移とか TransitionManager 使えばできるしキー入力の制御はできないしわざわざ Fragment を使う理由が無いよなー、と Fragment に対して消極的になっていきます。効果的に使える場面もあって、例えば DialogFragment のように Fragment 単体で機能を Library として提供するとかはあるのですが、結構限定的ですよね、、、</p><p>ということで ViewGroup (主に ConstraintLayout) を extends して Container-Component のような View を作成して Activity から直接扱うようにしてみたら、結構うまく動いてしまったのでしばらくそれで開発していきました。</p><p>時が流れ Jetpack が充実し画面遷移には Navigation Component も使っていきます。 Navigation Component を使うと Android Studio からは Storyboard 的な UI で画面遷移を視覚的にわかりやすくなり、画面遷移の実装は自動生成されるようになります。</p><p>で、この Navgation Component 、 Fragment を使うのである。</p><p>また Fragment か! と思いつつ、とりあえず Container-Component ライクな View はそのままにして Fragment は onCreateView だけ override して使うようにすると、とりあえず Navigation Component はうまく動いてくれます。 Fragment の LifecycleOwner が欲しい場合も</p><pre><code class="language-kotlin">fun View.getParentLifecycleOwner(): LifecycleOwner {
    var v = this
    while (true) {
        // via FragmentManager.findFragment.
        v.getTag(androidx.fragment.R.id.fragment_container_view_tag).let {
            if (it is Fragment) {
                return it.viewLifecycleOwner
            }
        }

        v = when (val p = v.parent) {
            is View -&gt; p
            else -&gt; break
        }
    }

    var context = this.context
    while (true) {
        context = when (context) {
            is AppCompatActivity -&gt; return context
            is ContextWrapper -&gt; context.baseContext
            else -&gt; throw RuntimeException(context.toString())
        }
    }
}
</code></pre><p>として、 View の Constructor の次の tick (View.post とか) で取得すれば解決してしまうんですね。</p><p>そのほか Navigation Component 使うと xml とかに実装散らばるし、そのうち飽きて元の実装に戻るんじゃないか、という懸念もだきつつとりあえず 1年ほど使いながら様子を見ています。ここ 1年で Jetpack がだいぶ充実し Jetpack が使えそうな個所は使うと良さそうという風潮もありますし、そろそろ Fragment を使うように戻してもいいのかな。</p><p>Jetpack Compose が普及したら Fragment は必須といった感じに評価が変わりそうな気がする。</p><hr/><p>timestamp<br/>
<!-- -->2021-04-25 (First edition)</p>]]></description>
      <pubDate>Sun Apr 25 2021 00:00:00 GMT+0000 (Coordinated Universal Time)</pubDate>
    </item>
    <item>
      <title>OP-TEE に入門する</title>
      <link>https://sukawasatoru.com/docs/2021-04-25-op-tee</link>
      <guid>https://sukawasatoru.com/docs/2021-04-25-op-tee</guid>
      <description><![CDATA[<h1>OP-TEE に入門する</h1><p>機会があって <a href="https://source.android.com/security/trusty">Trusty TEE</a> を勉強していたけれど <a href="https://optee.readthedocs.io">OP-TEE</a> を使うことになりそうなので、とりあえず Build できるまで急遽勉強をした。</p><p>OP-TEE のドキュメントを読めばなんとかなるかなーと思っていたら最新に追従できておらず <a href="https://source.android.com/">AOSP</a> や <a href="https://wiki.qemu.org/Hosts/Linux">QEMU</a> も合わせて読むことでなんとか QEMU まで実行できた、とハードルは高めでした。</p><p>とりあえず作ったものと Build までの手順は <a href="https://github.com/sukawasatoru/my-op-tee">GitHub</a> にまとめた。テストはそのうち試してみたい。</p><hr/><p>timestamp<br/>
<!-- -->2021-04-25 (First edition)</p>]]></description>
      <pubDate>Sun Apr 25 2021 00:00:00 GMT+0000 (Coordinated Universal Time)</pubDate>
    </item>
    <item>
      <title>Android で 10万件の写真を表示する</title>
      <link>https://sukawasatoru.com/docs/2021-03-28-android-photogallery</link>
      <guid>https://sukawasatoru.com/docs/2021-03-28-android-photogallery</guid>
      <description><![CDATA[<h1>Android で 10万件の写真を表示する</h1><p>Google Photo のような App を作る場合どう設計をするか。</p><p>iPhone の Photos もですが上下に非常に長いリストに Server から取得した Item が並ぶ UI を作るときにどう設計するか。 List はとりあえずで RecyclerView を使えば良さそうだし画像の表示に関しては Glide を使えば {memory ,disk }cache もいい感じにやってくれるので深く考えることはなさそうですね。気になる点は RecyclerView で使用する Adapter に持たせる Item の総数が非常に多くなった場合 OOM が発生しそうです。</p><p>これを解決するためにはまず App 起動時の Adapter は空の List を持ち Item の総数だけ知っている状態にし、 RecyclerView の現在地周辺の Item を Load して離れている場所の Item を忘れて、、、といった実装が必要そうです。 List を動的に操作する実装は過去それなりにやってきたのですがあまりやりたくない実装ですね。</p><p>そういえば <a href="https://developer.android.com/topic/libraries/architecture/paging/v3-overview">Paging library</a> あったけどあれ使えば実装が簡単になるかなー、とふと思ったので試してみました。</p><h2>静的に Load する</h2><p>いきなり Paging library に取り掛かる前にまずシンプルな実装をしてみましょう。</p><p>今回扱う Item は id / 作成日時 / 写真の url / 説明文 の 4つを扱うこととします。</p><pre><code class="language-kotlin">data class MyImage(
        val id: String,
        val createdDate: ZonedDateTime,
        val url: String,
        val description: String,
)
</code></pre><p>App は Server から Item を取得するので Server を実装します。 Rust + <a href="https://github.com/hyperium/hyper">Hyper</a> + <a href="https://github.com/graphql-rust/juniper">Juniper</a> で <a href="https://github.com/sukawasatoru/android-photogallery/blob/master/tools/server/src/main.rs">適当に実装します</a>。</p><img alt="" src="/2021-03-28-graphiql.png" height="460"/><p>Android から GraphQL を扱うために今回は <a href="https://www.apollographql.com/docs/android/">Apollo</a> を使います。</p><p>Apollo の詳細な使用方法は省略しますが <a href="https://github.com/sukawasatoru/android-photogallery/tree/master/data/graphql/src/main/graphql/jp/tinyport/photogallery/data/graphql">こんな感じに</a> schema.json と GraphiQL で作った .graphql ファイルを用意し通信に使用するファイルを自動生成します。</p><p>Apollo の準備が整ったら DataSource を実装します。</p><pre><code class="language-kotlin">interface ImageServerDataSource {
    suspend fun getImages(first: Int, after: String?): Result&lt;Pair&lt;List&lt;MyImage&gt;, String?&gt;, String&gt;
}

class ImageServerDataSourceImpl(apiEndpoint: String) : ImageServerDataSource {
    private val apolloClient = ApolloClient.builder()
            .serverUrl(apiEndpoint)
            .addCustomTypeAdapter(CustomType.DATETIMEUTC, object : CustomTypeAdapter&lt;ZonedDateTime&gt; {
                override fun decode(value: CustomTypeValue&lt;*&gt;): ZonedDateTime {
                    return ZonedDateTime.parse(value.value.toString(), DateTimeFormatter.ISO_DATE_TIME)
                }

                override fun encode(value: ZonedDateTime): CustomTypeValue&lt;*&gt; {
                    return CustomTypeValue.GraphQLString(value.format(DateTimeFormatter.ISO_DATE_TIME))
                }
            })
            .build()

    @RequiresPermission(Manifest.permission.INTERNET)
    override suspend fun getImages(first: Int, after: String?):
            Result&lt;Pair&lt;List&lt;MyImage&gt;, String?&gt;, String&gt; {
        return runCatching {
            val response = apolloClient.query(ImageMetaQuery(
                    first = first, after = Input.optional(after)))
                    .toFlow()
                    .first()

            response.errors?.let { errors -&gt;
                // TODO: parse error.
                return@runCatching Err(&quot;failed to retrieve data: $errors&quot;)
            }

            val dataImages = response.data!!.images!!
            val images = dataImages.nodes.map(MyImage::from)

            if (dataImages.pageInfo.hasNextPage) {
                Ok(Pair(images, dataImages.pageInfo.endCursor))
            } else {
                Ok(Pair(images, null))
            }
        }.getOrElse {
            if (it is ApolloHttpException) {
                return@getOrElse Err(&quot;failed to retrieve data: ${it.rawResponse()}&quot;)
            }
            Err(&quot;failed to retrieve data: $it&quot;)
        }
    }
}

private fun MyImage.Companion.from(entity: ImageMetaQuery.Node): MyImage {
    return MyImage(
            id = entity.id,
            createdDate = entity.createdDate,
            url = entity.url,
            description = entity.description,
    )
}
</code></pre><p>とくに特別なことをしていないですが戻り値について補足すると、戻り値に Result を使用しているのは Fallible な命令であることを明示したいためです。 throw を使っていないのは <a href="https://github.com/dtolnay/anyhow">Anyhow</a> とか <a href="https://github.com/rust-lang-nursery/failure">failure</a> の使い心地が良かったので Kotlin でも同じことしたかったからで Throwable を String にしているのは特に詳細が必要でないので手抜きしているためです。必要になったときに sealed な Error 型を作ります。</p><p>次に Cache のために <a href="https://developer.android.com/training/data-storage/room">Room</a> を用意します。</p><pre><code class="language-kotlin">@Dao
interface ImageDao {
    @Query(&quot;delete from image&quot;)
    fun deleteAll()

    @Query(&quot;SELECT * FROM image ORDER BY createdDate&quot;)
    fun findAll(): List&lt;ImageEntity&gt;

    @Insert
    fun saveImages(entities: List&lt;ImageEntity&gt;)

    @Transaction
    fun replaceAll(images: List&lt;ImageEntity&gt;) {
        deleteAll()
        saveImages(images)
    }
}

@Entity(tableName = &quot;image&quot;)
data class ImageEntity(
        @PrimaryKey
        override val id: String,
        @ColumnInfo(index = true)
        override val createdDate: ZonedDateTime,
        override val url: String,
        override val description: String,
)

class Converters {
    @TypeConverter
    fun fromZonedDateTime(value: ZonedDateTime): Long {
        return value.toInstant().toEpochMilli()
    }

    @TypeConverter
    fun intoZonedDateTime(value: Long): ZonedDateTime {
        return ZonedDateTime.ofInstant(Instant.ofEpochMilli(value), ZoneId.of(&quot;UTC&quot;))
    }
}
</code></pre><p>Room も GraphQL と同様に DataSource を実装します。</p><pre><code class="language-kotlin">interface ImageLocalDataSource {
    suspend fun getImages(): Result&lt;List&lt;MyImage&gt;, String&gt;

    suspend fun replaceImages(images: List&lt;MyImage&gt;): Result&lt;Unit, String&gt;
}

class ImageLocalDataSourceImpl(
        context: Context,
        private val db: ImageDatabase) : ImageLocalDataSource {
    private val log = EntryPointAccessors.fromApplication(context, DbEntryPoint::class.java)
            .log()

    override suspend fun getImages(): Result&lt;List&lt;MyImage&gt;, String&gt; {
        log.debug(&quot;[ImageLocalDataSource] getImages&quot;)

        return runCatching {
            Ok(db.imageDao().findAll().map(MyImage::from))
        }.getOrElse {
            Err(it.toString())
        }
    }

    override suspend fun replaceImages(images: List&lt;MyImage&gt;): Result&lt;Unit, String&gt; {
        log.debug(&quot;[ImageLocalDataSource] replaceImages&quot;)

        return runCatching {
            db.imageDao().replaceAll(images.map(MyImage::toEntity))
            Ok(Unit)
        }.getOrElse {
            Err(it.toString())
        }
    }
}

private fun MyImage.Companion.from(entity: ImageEntity): MyImage {
    return MyImage(
            id = entity.id,
            createdDate = entity.createdDate,
            url = entity.url,
            description = entity.description,
    )
}

private fun MyImage.toEntity(): ImageEntity {
    return ImageEntity(
            id = id,
            createdDate = createdDate,
            url = url,
            description = description,
    )
}
</code></pre><p>GraphQL と Room の DataSource ができました。これらを組み合わせて Repository を実装します。</p><pre><code class="language-kotlin">interface ImageRepository {
    suspend fun retrieveImageAndUpdate(): Flow&lt;Result&lt;List&lt;MyImage&gt;, String&gt;&gt;
}

class DefaultImageRepository @Inject constructor(
        @ApplicationContext
        private val context: Context,
        @RepositoryDispatcher
        private val dispatcher: CoroutineDispatcher,
        private val remoteDataSource: ImageServerDataSource,
        private val localDataSource: ImageLocalDataSource,
) : ImageRepository {
    private val log = EntryPointAccessors.fromApplication(context, RepositoryEntryPoint::class.java)
            .log()

    override suspend fun retrieveImageAndUpdate():
            Flow&lt;Result&lt;List&lt;MyImage&gt;, String&gt;&gt; {
        return flow {
            when (val data = localDataSource.getImages()) {
                is Ok -&gt; emit(Ok(data.value))
                is Err -&gt; {
                    emit(Err(data.error))
                    return@flow
                }
            }

            val images = mutableListOf&lt;MyImage&gt;()
            var after: String? = null
            while (true) {
                val (retImages, cursor) = when (
                    val data = remoteDataSource.getImages(1000, after)) {
                    is Ok -&gt; {
                        data.value
                    }
                    is Err -&gt; {
                        emit(Err(&quot;failed to retrieve image: ${data.error}&quot;))
                        return@flow
                    }
                }
                log.info(&quot;@@@@ succeeded&quot;)
                images.addAll(retImages)
                if (cursor == null) {
                    break
                }
                after = cursor
            }
            emit(Ok(images))

            when (val data = localDataSource.replaceImages(images)) {
                is Ok -&gt; {
                    // do nothing.
                }
                is Err -&gt; {
                    emit(Err(data.error))
                }
            }
        }.flowOn(dispatcher)
    }
}
</code></pre><p>この <em>retrieveImageAndUpdate</em> は 1回目の emit は Room のデータを、 2回目は GraphQL のデータを 1000件ずつリクエストし全件取得したものをまとめて emit し、 emit 後に終了する Flow を返します。 Repository で <code>flowOn(dispatcher)</code> をしていますがそれに使用する Dispatcher は <a href="https://developer.android.com/kotlin/coroutines/coroutines-best-practices#inject-dispatchers">Best practices</a> に従って Inject されたものを使用します。</p><p>これで静的に Load するための実装ができました。 RecyclerView の実装は ListView からお馴染みの <a href="https://github.com/sukawasatoru/android-photogallery/blob/508e793de316e49fdcdb369364fd8bf349c02e14/app/src/main/kotlin/jp/tinyport/photogallery/MainActivity.kt#L115-L153">シンプルな MVP</a> で大丈夫です。</p><p>では App を起動しつつ Logcat を確認してみましょう。予想通りであれば 1000件取得するごとに <em>@@@@ succeeded</em> が出力されるので、それがしばらく表示されたのちに OOM します。</p><video controls="" width="832"><source src="/2021-03-28-logcat.mp4" type="video/mp4"/></video><p>40秒あたりから Blocking GC が呼び出されるようになり 1分あたりからほとんど身動きが取れなくなって OOM していますね。</p><h2>Paging library を使用する</h2><p>前置きが長くなりましたが Paging library を使ってみましょう。</p><p>簡単に実装するため今回は GraphQL から取得するだけで Room で Cache は考えないようにします。 DataSource は先ほど作ったものを流用します。</p><p>そのほか Paging library で List を更新するためには RecyclerView.Adapter を Wrap した Adapter とそれの為の Pager という class を実装すれば良さそうです。</p><pre><code class="language-kotlin">class MainActivity : AppCompatActivity() {
    private fun usePagingV3() {
        val adapter = MyPagingAdapter()
        binding.list.adapter = adapter

        lifecycleScope.launch {
            pagingVm.pagerFlow.collectLatest { adapter.submitData(it) }
        }
    }
}

@HiltViewModel
internal class PagingVm @Inject constructor(repo: ImageRepository) : ViewModel() {
    val pagerFlow = repo.pagingV3Stream().cachedIn(viewModelScope)
}

class DefaultImageRepository {
    override fun pagingV3Stream(): Flow&lt;PagingData&lt;MyImage&gt;&gt; {
        return Pager(PagingConfig(pageSize = 1000)) {
            object : PagingSource&lt;String, MyImage&gt;() {
                val firstKey = UUID.randomUUID().toString()
                val keys = mutableSetOf(firstKey)

                override suspend fun load(params: LoadParams&lt;String&gt;): LoadResult&lt;String, MyImage&gt; {
                    log.info(&quot;[ImageRepository] load key: %s, loadSize: %s&quot;,
                            params.key, params.loadSize)
                    // for load between 0 and &quot;after&quot; index.
                    val key = if (params.key == firstKey) {
                        null
                    } else {
                        params.key
                    }
                    return when (val data = remoteDataSource.getImages(params.loadSize, key)) {
                        is Ok -&gt; {
                            data.value.second?.let {
                                keys.add(it)
                            }
                            var prevKey: String? = null
                            for ((index, entry) in keys.withIndex()) {
                                if (entry == key) {
                                    prevKey = keys.elementAtOrNull(index - 1)
                                    break
                                }
                            }
                            log.info(&quot;[ImageRepository] load prevKey: %s, key: %s, nextKey: %s&quot;,
                                    prevKey, key, data.value.second)
                            LoadResult.Page(data.value.first, prevKey, data.value.second)
                        }
                        is Err -&gt; LoadResult.Error(Exception(data.error))
                    }
                }

                override fun getRefreshKey(state: PagingState&lt;String, MyImage&gt;): String? {
                    return state.anchorPosition?.let { anchorPosition -&gt;
                        val closest = state.closestPageToPosition(anchorPosition)
                        closest?.prevKey ?: closest?.nextKey
                    }
                }
            }
        }
                .flow
                .flowOn(dispatcher)
    }
}
</code></pre><p>PageSource.load() ですが、ここで GraphQL の DataSource を使用したデータを読み方を実装します。</p><p>LoadParams.key に Paging に使用する情報、 GraphQL では <a href="https://graphql.org/learn/pagination/#pagination-and-edges">after (Cursor)</a> が key に設定されるのでこれを使用して GraphQL から必要な位置の Item を取得します。 App 起動後の初回読み込みの LoadParams.key は null です。</p><p>読み込んだ Item は LoadResult.Page(items, prev, next) で返します。 LoadResult.Page には Item の他に GraphQL で使用する為の現在地の前後の after を渡すことができます。現在地より前または後ろに Items がなく Paging できない場合は null を渡す必要があります。上の実装で <code>if (params.key == firstKey) { null } else { params.key }</code> をしているのは初めのページを表現するために null を使用できないためですね。</p><p>では、最低限 Paging library に必要な実装はできたので App を起動してひたすらスクロールしてみましょう。こちらも OOM するでしょうか。</p><video controls="" width="832"><source src="/2021-03-28-logcat-paging.mp4" type="video/mp4"/></video><p>何回か試しましたが 30万件前後で OOM します。なんだ Paging library もダメなのか、、、と思いながらドキュメントを眺めていると PagingConfig に <em>maxSize</em> というそれっぽい Field がありました。こちらを試すと Blocking GC が発生せず軽々と 30万件を読み込むことができました。よかったですね。</p><pre><code class="language-kotlin">override fun pagingV3Stream(): Flow&lt;PagingData&lt;MyImage&gt;&gt; {
    return Pager(PagingConfig(
            pageSize = 1000,
            initialLoadSize = 1000,
            maxSize = 5000,
    )) {
    // snip
}
</code></pre><p>ちなみに <em>maxSize</em> に合わせ <em>initialLoadSize</em> も設定していますが、こちらは Paging library のバグなのか <em>pazeSize</em> と同じ値にしないと上に戻ったときに Adapter の index がおかしくなり戻ることができなくなるため設定しています。</p><h2>RemoteMediator を使用する</h2><p>上記実装では GraphQL から値を取得しますが <em>maxSize</em> 以上取得した場合次々と古いデータを忘れていくため、スクロールで戻ったときには再び GraphQL からデータを取得する必要があります。 Paging library を使う前の実装のように Room を使用して取得したデータを Cache するようにしてみましょう。</p><p>Pager にはこれを実現するために RemoteMediator という仕組みがあります。 RemoteMediator とは Room の PagingSource と GraphQL の PagingSource を用意すると Library 側でいい感じに GraphQL から読み込んだり Room から読み込んだりしてくれます。</p><pre><code class="language-kotlin">class DefaultImageRepository {
    override fun imageStream(): Flow&lt;PagingData&lt;MyImage&gt;&gt; {
        return Pager(
                config = PagingConfig(
                        pageSize = 1000,
                        maxSize = 5000,
                ),
                remoteMediator = object : RemoteMediator&lt;Int, MyImage&gt;() {
                    val nextKeys = mutableSetOf&lt;String&gt;()

                    override suspend fun load(
                            loadType: LoadType,
                            state: PagingState&lt;Int, MyImage&gt;): MediatorResult {
                        val after = when (loadType) {
                            LoadType.REFRESH -&gt; {
                                log.info(&quot;[ImageRepository] loadType: %s, pageSize: %s&quot;,
                                        loadType.name, state.config.pageSize)
                                null
                            }
                            LoadType.PREPEND -&gt; {
                                log.info(&quot;[ImageRepository] loadType: %s&quot;, loadType.name)
                                return MediatorResult.Success(true)
                            }
                            LoadType.APPEND -&gt; {
                                log.info(&quot;[ImageRepository] loadType: %s, pageSize: %s, nextKeys: %s&quot;,
                                        loadType.name, state.config.pageSize, nextKeys.lastOrNull())
                                if (state.lastItemOrNull() == null) {
                                    return MediatorResult.Success(false)
                                }
                                nextKeys.lastOrNull()
                            }
                        }
                        val (data, nextKey) = when (
                            val data = remoteDataSource.getImages(state.config.pageSize, after)) {
                            is Ok -&gt; data.value
                            is Err -&gt; return MediatorResult.Error(Exception(data.error))
                        }

                        if (loadType == LoadType.REFRESH) {
                            localDataSource.replaceImages(data)
                        } else {
                            localDataSource.saveImages(data)
                        }

                        nextKey?.let {
                            nextKeys.add(it)
                        }

                        return MediatorResult.Success(nextKey == null)
                    }
                },
                pagingSourceFactory = { localDataSource.pagingSource() }
        )
                .flow
                .flowOn(dispatcher)
    }
}

class ImageLocalDataSourceImpl {
    override fun pagingSource(): PagingSource&lt;Int, MyImage&gt; {
        log.debug(&quot;[ImageLocalDataSource] pagingSource&quot;)

        // automatically generated PagingSource uses LimitOffsetDataSource.java.
        // it generates `SELECT * FROM ( &quot; + mSourceQuery.getSql() + &quot; ) LIMIT ? OFFSET ?`
        // so need to create the index for avoiding the table scan.
        return db.imageDao().pagingSource() as PagingSource&lt;Int, MyImage&gt;
    }
}

@Dao
interface ImageDao {
    // snip.

    @Query(&quot;SELECT * FROM image ORDER BY createdDate&quot;)
    fun pagingSource(): PagingSource&lt;Int, ImageEntity&gt;

    // snip
}
</code></pre><p>RemoteMediator の基本的な実装は今までやってきた PagingSource の実装と大体同じです。ポイントとしては LoadParams.key がなくなり LoadType と PagingState.lastItemOrNull() で after (Cursor) を決める必要がある、取得した Item は自分で Room に保存する必要があることでしょうか。</p><p>LoadType は REFRESH が App を起動してからの初期読み込みに使用されます (Adapter に refresh の命令をすれば使われますが今回は使っていません)。 PREPEND は List の先頭から Item を読み込む場合は初期読み込み時にしか呼び出されないため常に endOfPagenationReached = true として問題ないです。 APPEND は after を使用しての読み込みですね。 <code>if (state.lastItemOrNull() == null)</code> をしているのは初期読み込み中に APPEND が呼ばれることがあるのでそのときに何もしない為の実装です。</p><p>Room の方の PagingSource は DAO の自動生成の実装を使用します。</p><h2>まとめ</h2><p>Paging library を使用することで巨大な Adapter を実現することができました。ただちょっと気になる点もありどうやら Item の更新は大丈夫なのですが追加や削除をした際に動作が安定しない感じがします。</p><p>そのほか最近だと巨大な Adapter を持つ App は少なく、例えば Twitter だと一定数以上読み込むと古いデータを忘れるようになっていたりするので、実際の App 開発で Paging library を使うシーンはあまりないのかなー、という気はしています。</p><p>長いリストを持っても最初はスクロール楽しいですが、疲れますしね。</p><hr/><p>timestamp<br/>
<!-- -->2021-03-28 (First edition)</p>]]></description>
      <pubDate>Sun Mar 28 2021 00:00:00 GMT+0000 (Coordinated Universal Time)</pubDate>
    </item>
    <item>
      <title>Android 向けの Unity に入門する</title>
      <link>https://sukawasatoru.com/docs/2021-02-23-android-unity</link>
      <guid>https://sukawasatoru.com/docs/2021-02-23-android-unity</guid>
      <description><![CDATA[<h1>Android 向けの Unity に入門する</h1><p>Unity を使えるとできることの幅が大きく広がるので Android 上で動かしてみました。</p><p>210223時点での Unity 歴は (Ubuntu で起動した Unity の) ロボットのシミュレーターに使用する Plugin を書いたことがある程度でほとんど分かっていないです。</p><p>とりあえず <a href="https://github.com/Unity-Technologies/uaal-example">GitHub のサンプルコード uaal-example</a> と <a href="https://github.com/Unity-Technologies/uaal-example/blob/master/docs/android.md">そのドキュメント</a> と <a href="https://docs.unity3d.com/2020.2/Documentation/Manual/android.html">公式のドキュメント</a> が用意されているのでそれに従って進めていきます。</p><p>Android 上で起動するだけであれば GitHub だけを見ていれば十分で unity3d.com の方は参考程度に読めば大丈夫でした。</p><p>今回作ったものは <a href="https://github.com/sukawasatoru/android-hello-unity">GitHub</a> に push してあります。</p><h2>Ubuntu 上に Unity を Install する</h2><p><a href="https://docs.unity3d.com/2020.2/Documentation/Manual/android-sdksetup.html">Unity - Manual: Android environment setup</a> に従えばいいのでわざわざ Install 手順を書かなくても、、と思ったのですが Ubuntu で実行する場合に 1つポイントがありました。 Unity Hub と適当な Version の Unity を Install し <em>Projects</em> Tab から <em>ADD</em> で適当な Project を新規作成する必要があります。何でもいいのでまず新規作成をしないとなぜか既存の Project が開けませんでした。</p><p>macOS からだと特にそんなことはないですし Ubuntu から Unity を使う人は珍しいと思いますが、、、</p><p>そのほか手元の Ubuntu 20.04 では <em>libgconf-2-4</em> が足りていなかったので <code>sudo apt install libgconf-2-4</code> で入れてしまいます。</p><h2>既存の Gradle で構成された Android Project に組み込む</h2><p>Android Project は適当に用意しておきます。 Directory 構成は <a href="https://github.com/Unity-Technologies/uaal-example">uaal-example</a> を参考に次のようにしました:</p><pre><code>`- project-root
  `- app // これが App の本体
    `- build.gradle      // App の Gradle script / これを編集して unityLibrary を組み込む
  `- UnityProject        // Unity で開く Project
    `- androidBuild      // Unity で Export した Android Project
      `- launcher        // Unity から Export された Project のひとつ / 今回は使用しない
      `- unityLibrary    // Unity から Export された Project のひとつ / 今回はこれを app に組み込む
      `- build.gradle    // Unity から Export された Gradle script / これを使用しないようにする必要がある
      `- settings.gradle // Unity から Export された Gradle script / これを使用しないようにする必要がある
  `- build.gradle        // Project の Root の Gradle script
  `- settings.gradle     // Project の Root の Gradle script / これを編集して unityLibrary を組み込む
</code></pre><p>Android 本体の module <em>app</em> から使用するのは Export した <em>UnityProject/androidBuild/unityLibrary</em> だけです。 <em>unityLibrary</em> を Gradle project に設定するのも 1つポイントがあります。</p><p><a href="https://github.com/Unity-Technologies/uaal-example/blob/master/docs/android.md">GitHub のドキュメント</a> にはさらっと書いてありましたが Root の <em>settings.gradle</em> には</p><pre><code class="language-groovy">include &#x27;:app&#x27;, &#x27;:unityLibrary&#x27;
project(&#x27;:unityLibrary&#x27;).projectDir = file(&#x27;UnityProject/androidBuild/unityLibrary&#x27;)
</code></pre><p>といったように projectDir を設定する必要があります。これは Unity から Export される Project は他の Project から Import されるように Gradle script が書かれていないからです。たとえば Root の <em>settings.gradle</em> で <code>include &#x27;:UnityProject:androidBuild:unityLibrary&#x27;</code> というように設定すると <em>UnityProject/androidBuild/settings.gradle</em> が読み込まれてしまうので、これを回避する必要があります。</p><h2>最新の Gradle を使用する</h2><p><a href="https://docs.unity3d.com/2020.2/Documentation/Manual/android-gradle-overview.html">unity3d.com のドキュメント</a> を見ると現時点で最新の Unity v2020.2 で使用する Gradle の Version は 5.6.4 です。</p><p>これはちょっといやな感じがするところで Gradle を使用して Android を Build するには AGP (Android Gradle plugin) を使用する必要が有り AGP を使用するには基本的に <a href="https://developer.android.com/studio/releases/gradle-plugin#updating-gradle">対応している Version の Gradle</a> を使用する必要があります (違う Version の Gradle を使うこともできるのですが Gradle は Minor version が上がってもよく壊れるので、、、)。</p><p><a href="https://developer.android.com/studio/releases/gradle-plugin#updating-gradle">対応している Version の List</a> をみると Gradle v5.6.4 を使用する場合 AGP v3.6.4 を使用する必要があります。ギリギリ View Binding にも対応しますし非常に困るといったことはなさそうですが、基本的に Gradle は Version が上がると Build が速くなりますし、 AGP も新しくしないと最新の OS を targetSdkVersion に指定できなかったり Android Studio から最新の機能を使えなかったりするので AGP は最新 (少なくとも 210223 現在安定している 4.0.2) を使いたいですよね。</p><p>何とかできないか Unity で Export した Gradle script を見たら 5.6.4 でないといけない理由が無さそうでした。ということで最新の AGP とそれに対応する Gradle を使ってみると問題なく Build できました。</p><p>unity3d.com のドキュメントにある Version は Unity が Template にしようしている Gradle の Version がたまたま 5.6.4 だっただけみたいですね。</p><h2>必要な Android Resources を追加する</h2><p>UnityPlayer を new したときに Android の string を読み込むので Resource を用意してあげないと new した瞬間に落ちます。 strings.xml に値を定義しましょう:</p><pre><code class="language-xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;utf-8&quot; ?&gt;
&lt;resources&gt;
    &lt;string name=&quot;game_view_content_description&quot;&gt;Game view&lt;/string&gt;
&lt;/resources&gt;
</code></pre><h2>CI で Build する</h2><p>TODO: 後で書く</p><h2>Android TV で動かす</h2><p>Android 向け Unity は v2019.3 以降 arm しか対応していないようです。ですので Nexus Player は Atom なので動きません。なってこった！</p><p>2020年に発売された <a href="https://store.google.com/product/chromecast_google_tv">Chromecast with Google TV</a> または <a href="https://www.sony.jp/bravia/">BRAVIA</a> を買いましょう。ちなみに 210223現在の BRAVIA は armv7a です。</p><p>refs.<br/>
<a href="https://github.com/Unity-Technologies/uaal-example">https://github.com/Unity-Technologies/uaal-example</a><br/>
<a href="https://github.com/Unity-Technologies/uaal-example/blob/master/docs/android.md">https://github.com/Unity-Technologies/uaal-example/blob/master/docs/android.md</a><br/>
<a href="https://docs.unity3d.com/2020.2/Documentation/Manual/android.html">https://docs.unity3d.com/2020.2/Documentation/Manual/android.html</a><br/>
<a href="https://developer.android.com/studio/releases/gradle-plugin#updating-gradle">https://developer.android.com/studio/releases/gradle-plugin#updating-gradle</a><br/>
<a href="https://store.google.com/product/chromecast_google_tv">https://store.google.com/product/chromecast_google_tv</a><br/>
<a href="https://www.sony.jp/bravia/">https://www.sony.jp/bravia/</a></p><hr/><p>timestamp<br/>
<!-- -->2021-02-23 (First edition)</p>]]></description>
      <pubDate>Tue Feb 23 2021 00:00:00 GMT+0000 (Coordinated Universal Time)</pubDate>
    </item>
    <item>
      <title>Next.js で RSS を SSG で生成する</title>
      <link>https://sukawasatoru.com/docs/2021-02-15-nextjs-rss</link>
      <guid>https://sukawasatoru.com/docs/2021-02-15-nextjs-rss</guid>
      <description><![CDATA[<h1>Next.js で RSS を SSG で生成する</h1><p>ここ数年で更新通知に Twitter が利用されることが多くなり RSS に対応しているサービスが少なくなってきていますが、私は iPhone の <a href="https://apps.apple.com/jp/app/reeder-5/id1529445840">Reeder</a> によく見るサイトの RSS を登録しておき移動中に閲覧するといった使い方をしています。まだ需要があるという人もそれなりにいるのではないでしょうか。</p><p>そんな RSS ですが、普及して欲しいと思っている割には自分のサイトは未対応でしたので Next.js の SSG の機能に細工をして自動生成するようにしてみました。</p><h2>RSS を生成する CLI を作成する</h2><p>RSS は XML の専用のフォーマットが定められているため何も考えずに Next.js で書いた場合 <code>&lt;html&gt;</code> タグがついてしまったり、 RSS のフォーマットで出力することができません。それを回避するために <em>getServerProps</em> を使う方法があるのですが GitHub Pages に静的に Commit したいので、その場合 <em>getServerProps</em> は使えません。そのほか <!-- -->_<!-- -->app.tsx や <!-- -->_<!-- -->document.tsx に手を加えたらできないことはないと思いますが、大掛かりになりそうでしたので CLI 版を作成しました。</p><p>構成は ts-node を使用して TypeScript で実装を行い yargs で CLI の引数を Parse します。 RSS に使用する情報は <a href="/docs/2021-02-11-wasm">以前作成した Wasm</a> を CommonJS で読み込めるように出力するようにして、、とすることで実装できましたが何か冗長な感じが拭えません。</p><p> <a href="https://github.com/sukawasatoru/blog/commit/12d3f674695068f3b3871d497672b103eb0a6a8c">Add RSS support · sukawasatoru/blog@12d3f67</a></p><p> もっと単純に実装できないでしょうか</p><h2>Next.js の getStaticProps で RSS を生成する</h2><p>ここでふと src/pages/feed.tsx を作成し、その <em>getStaticProps</em> で public/feed.xml に <em>fs.writeFile</em> してしまい FunctionComponent からは <code>&lt;meta http-equiv=&quot;refresh&quot;&gt;</code> を使って /feed.xml に Redirect すればいいんじゃないか、ということを思いついたのでおもむろに試したら動きました。</p><p>この方法であれば writeFile に何となくすっきりしないものを感じますが Wasm を CommonJS で出力するようにしたり CLI を追加することなく feed.tsx をあたらに実装するだけで RSS を生成することができます。</p><pre><code class="language-typescript">// src/pages/feed.tsx
const Feed: FunctionComponent = () =&gt; {
  return &lt;Head&gt;
    &lt;meta httpEquiv=&quot;refresh&quot; content=&quot;0; url=/feed.xml&quot;/&gt;
    &lt;title&gt;redirect to feed.xml&lt;/title&gt;
  &lt;/Head&gt;;
};

export const getStaticProps: GetStaticProps = async () =&gt; {
  const baseUrl = `https://example.com`;

  // 以前 Wasm で作った Markdown parser
  const entries = (await retrieveDocs())
    .sort((a, b) =&gt; Temporal.PlainDate.compare(b.firstEdition, a.firstEdition));

  let buf = `&lt;?xml version=&quot;1.0&quot;?&gt;
&lt;rss version=&quot;2.0&quot;&gt;
  &lt;channel&gt;
    &lt;title&gt;sukawasatoru.com&lt;/title&gt;
    &lt;link&gt;${baseUrl}&lt;/link&gt;
    &lt;description&gt;My description&lt;/description&gt;
    &lt;lastBuildDate&gt;${new Date()}&lt;/lastBuildDate&gt;
`;

  for (const entry of entries) {
    const target = `${baseUrl}/docs/${entry.stem}`;
    const mdxSource = await renderToString((await readFile(entry.filepath)).toString());

    buf += `    &lt;item&gt;
      &lt;title&gt;${entry.title}&lt;/title&gt;
      &lt;link&gt;${target}&lt;/link&gt;
      &lt;guid&gt;${target}&lt;/guid&gt;
      &lt;description&gt;&lt;![CDATA[${mdxSource.renderedOutput}]]&gt;&lt;/description&gt;
      &lt;pubDate&gt;${new Date(entry.firstEdition.toString())}&lt;/pubDate&gt;
    &lt;/item&gt;
`;
  }

  buf += `  &lt;/channel&gt;
&lt;/rss&gt;`;

  await writeFile(`${process.cwd()}/public/feed.xml`, buf);
  return {
    props: {},
  };
};
</code></pre><p>いまのところこれで困ったことはなさそうですので、しばらくはこの仕組みを使って RSS を生成したいと思います。</p><hr/><p>timestamp<br/>
<!-- -->2021-02-15 (First edition)</p>]]></description>
      <pubDate>Mon Feb 15 2021 00:00:00 GMT+0000 (Coordinated Universal Time)</pubDate>
    </item>
    <item>
      <title>Rust と iced で GUI App を作る</title>
      <link>https://sukawasatoru.com/docs/2021-02-13-iced</link>
      <guid>https://sukawasatoru.com/docs/2021-02-13-iced</guid>
      <description><![CDATA[<h1>Rust と iced で GUI App を作る</h1><p>定量的に行う作業を楽にするためにツールを作ることは多々あると思います。ツールを作る場合 CLI 向けに作れば十分なことがほとんどだと思いますが、作業によっては GUI を持つツールの方が良いことがあると思います。</p><p>Android Emulator や実機の Android を操作するためにリモコンが欲しかったので
Rust と <a href="https://github.com/hecrj/iced">iced</a> を使用し GUI App を作成しました。</p><h2>WPF on PowerShell</h2><p>2013 年辺りでも PC から操作できるリモコンが欲しかったので当時は PowerShell で GUI App を作成していました。 PowerShell を選んだ理由は Windows 7 上で TextEditor があれば手軽に GUI が作れるからですね。</p><p>ボタンが押されたのでこのイベントを実行し Serial (UART) で送信、といったものをすべて PowerShell で記述し UI は別途 XAML を用意して PowerShell から読み込んで描画していました。</p><p>PowerShell は標準の Cmdlet では機能が足りないといった場合でも .NET であれば <code>Add-Type -AssemblyName PresentationFramework -ErrorAction SilentlyContinue</code> のようにすれば何でも使えるのが強いですね。</p><p>PowerShell の Cmdlet も <em>Out-GridView</em> を使えば何時何分にどのボタンをどれくらい長押しした、といった Object を渡せば Excel みたいにいい感じに表示してくれたり便利な機能がありいい感じです。</p><img alt="PowerShell で作ったリモコン" src="/2021-02-13-powershell-sircs.png" height="480"/><p>このリモコンはボタンが増えたりキーマクロのようにエージングテストの為に記録したキーを再生できるようにしたり、 Android を操作できるようにしたりと数年機能追加して活躍しました。</p><h2>iced on Rust</h2><p>使用する OS が Windows 10 になり、当時のコマンドプロンプトや PowerShell のキー入力が変わり、慣れ親しんだ操作感に戻すことができないため、たんだん PowerShell を使わなくなってしまいました。今では Windows Terminal があるのでそれ経由で PowerShell を使えば問題ないのですがメインの環境を MacBook Pro
移行し Bash 前提にカスタマイズした現状で満足しているので、今から PowerShell に戻ることはしたくないですね。</p><p>今はツールをすべて Rust で作っているので Rust で GUI を作れたら、、と探していたら <a href="https://github.com/hecrj/iced">iced</a> Crate を見つけました。</p><p>iced は Metal / DirectX / Vulkan / OpenGL を使用して cross-platform に対応している点が、現在 macOS / Linux / Windows を日常的に使っているため魅力的でした。また、以前 <a href="https://github.com/ocornut/imgui">ImGui</a> を使った経験があったので、何となく使い勝手が近いかな、とか start の数で何となくで使ってみようと思いました。</p><p>実装したものがこちらです:</p><p><a href="https://github.com/sukawasatoru/android-commander">Android Commander</a>  </p><img alt="Android Commander" src="/2021-02-13-android-commander.png" height="460"/><p>解説記事がとても少ないので example を読みながら Hello world を実装し、それをベースに 1日前後でつくりました。 PowerShell 版と比べると視認性があんまり良くないですし最低限の機能だけですがとりあえず動きます。</p><h3>iced で UI を作る</h3><p>iced で UI を表示するためには <a href="https://github.com/hecrj/iced#overview">iced の README</a> にあるように Application trait の <em>fn view()</em> を実装し Flexbox 的に Component を詰め込んだものを返します。</p><pre><code class="language-rust">struct MyApp {
    adb_connectivity: AdbConnectivity,
    adb_server_rx: tokio::sync::watch::Receiver&lt;String&gt;,
    adb_server_tx: tokio::sync::watch::Sender&lt;String&gt;,
    input_list: Vec&lt;SendEventDevice&gt;,
    pressed_key: std::collections::HashSet&lt;SendEventKey&gt;,
    sendevent_device: SendEventDevice,
    widget_states: WidgetStates,
}

impl Application for MyApp {
    // snip.
    fn view(&amp;mut self) -&gt; Element&lt;&#x27;_, Self::Message&gt; {
        let button_width = Length::Units(90);
        let button_height = Length::Units(30);

        Column::new()
            .push(
                Button::new(&amp;mut self.widget_states.adb_button, Text::new(&quot;devices&quot;))
                    .on_press(AppCommand::OnAdbButton),
            )
            .push(Checkbox::new(
                match self.adb_connectivity {
                    AdbConnectivity::Connecting | AdbConnectivity::Disconnected =&gt; false,
                    AdbConnectivity::Connected =&gt; true,
                },
                &quot;login&quot;,
                |_| AppCommand::OnAdbConnectClicked,
            ))
            .push(Text::new(match self.adb_connectivity {
                AdbConnectivity::Connecting =&gt; &quot;adb: connecting&quot;,
                AdbConnectivity::Connected =&gt; &quot;adb: connected&quot;,
                AdbConnectivity::Disconnected =&gt; &quot;adb: disconnected&quot;,
            }))
            .push(PickList::new(
                &amp;mut self.widget_states.picklist_device,
                self.input_list.as_slice(),
                Some(self.sendevent_device.clone()),
                AppCommand::TargetDeviceChanged,
            ))
            .push(
                Row::new()
                    .push(Space::new(button_width.clone(), button_height.clone()))
                    .push(
                        Button::new(&amp;mut self.widget_states.button_up, Text::new(&quot;Up (k)&quot;))
                            .width(button_width.clone())
                            .height(button_height.clone())
                            .on_press(AppCommand::RequestSendEvent(SendEventKey::KeyDpadUpClick)),
                    ),
            )
            // snip.
            .into()
    }
}
</code></pre><p>このように Component を詰めて UI を作っていきます。 Button が押されたりすると (Application を実装した) MyApp の値が更新され、ボタンが押されたという Event を <em>fn update()</em> によって知ることができるので、 update にボタンが押されたときの動作を実装していきます。</p><pre><code class="language-rust">fn update(&amp;mut self, message: Self::Message) -&gt; Command&lt;Self::Message&gt; {
    use AppCommand::*;

    match message {
        OnAdbButton =&gt; {
            info!(&quot;update OnAdbButton&quot;);
            return Command::perform(invoke_adb(), |_| AppCommand::InvokeAdbResult);
        }
        OnAdbConnectClicked =&gt; match self.adb_connectivity {
            AdbConnectivity::Disconnected =&gt; {
                self.adb_connectivity = AdbConnectivity::Connecting
            }
            AdbConnectivity::Connecting =&gt; {
                warn!(&quot;TODO&quot;);
            }
            AdbConnectivity::Connected =&gt; {
                self.adb_connectivity = AdbConnectivity::Disconnected;
                self.adb_server_tx.send(&quot;&quot;.into()).ok();
            }
        },
        RequestSendEvent(data) =&gt; {
            info!(&quot;update RequestSendEvent: {:?}&quot;, data);
            // snip.
        }
        TargetDeviceChanged(device) =&gt; {
            self.sendevent_device = device;
        }
    }
     Command::none()
}
</code></pre><p>簡単な App であれば  </p><ul><li>view() で状態を読み取り UI を作る<ul><li>update() で状態を更新する<ul><li>view() で状態を読み取り UI を作る<ul><li>update() で状態を更新する<ul><li>//</li></ul></li></ul></li></ul></li></ul></li></ul><p>というように view() と update() の Main Loop で動く App を作っていきます。</p><h3>非同期処理を実行する</h3><p>update() が呼ばれたときに HTTP でファイルをダウンロードするような IO が発生する処理や計算量の多い処理をする場合は上記コードの</p><pre><code class="language-rust">Command::perform(invoke_adb(), |_| AppCommand::InvokeAdbResult)
</code></pre><p>のように <em>Command::perform()</em> を使用します。これにより第一引数の <code>async { foo(); }</code> な関数を実行し、完了したら第二引数の <code>fn bar(第1引数の関数の戻り値) { baz } </code> が呼ばれ、その戻り値により update() が呼ばれます。つまり</p><ul><li>view() で Button を描画<ul><li>Button を押す<ul><li>update() が呼ばれ Command::perform(a, b) を実行<ul><li><em>a()</em> を実行<ul><li><em>a()</em> の戻り値により <em>b()</em> を実行<ul><li><em>b()</em> の戻り値により <em>update()</em> を実行<ul><li><em>update()</em> の引数により状態を更新<ul><li><em>view()</em> で状態を描画</li></ul></li></ul></li></ul></li></ul></li></ul></li></ul></li></ul></li></ul><p>ということになります。	</p><h3>長時間動く Background 処理を実装する</h3><p>UI に紐付かない長時間裏で動く処理、たとえば Android の Service のようなことをしたい場合は Subscription を使用します。</p><p>Subscription を使用することで裏の Worker thread で動き続けている処理から Rx の Subject のように値を Main thread に送り続ける、といったことができます。</p><p>iced の example では <a href="https://github.com/hecrj/iced/tree/master/examples/download_progress">Download progress</a> が Subscription を使用しています。 Download progress は裏でファイルのダウンロードをしつつ定期的に進捗を Main Thread に流すことで Progress bar を更新する、といったことをしています。</p><p>今回のリモコン App でもリモコン App が Android と通信するために adb を使用する箇所で Subscription を使用しています。 App の <strong>[ ]<!-- --> login</strong> のチェックが入ったら Subscription を起動し adb を常駐させ Main thread と通信を行います。</p><pre><code class="language-rust">fn subscription(&amp;self) -&gt; Subscription&lt;Self::Message&gt; {
    match self.adb_connectivity {
        AdbConnectivity::Connecting | AdbConnectivity::Connected =&gt; Subscription::batch(vec![
            Subscription::from_recipe(AdbServerRecipe {
                rx: self.adb_server_rx.clone(),
            })
            .map(AppCommand::AdbServerRecipeResult),
            iced_native::subscription::events().map(AppCommand::Event),
        ]),
        AdbConnectivity::Disconnected =&gt; Subscription::none(),
    }
}
</code></pre><p>この Subscription は rx (<code>tokio::sync::watch::Receiver&lt;String&gt;</code>) を抱えています。これは iced の仕組みとして Subscription から Main thread に値を流すことはできますが、逆に Main thread からすでに起動している Subscription に対して値を渡すことができないため、それを可能にするために抱えています。</p><p>Subscription の実装はちょっと長いですが次のようになります:</p><pre><code class="language-rust">#[derive(Clone, Debug)]
enum AdbServerRecipeEvent {
    Connected,
    Disconnected,
    Error,
}

enum AdbServerRecipeInternalState {
    Init(tokio::sync::watch::Receiver&lt;String&gt;),
    Ready(tokio::sync::watch::Receiver&lt;String&gt;, std::process::Child),
    Disconnecting,
    Finish,
}

impl&lt;H, I&gt; Recipe&lt;H, I&gt; for AdbServerRecipe
where
    H: std::hash::Hasher,
{
    type Output = AdbServerRecipeEvent;

    fn hash(&amp;self, state: &amp;mut H) {
        std::any::TypeId::of::&lt;Self&gt;().hash(state);
    }

    fn stream(self: Box&lt;Self&gt;, _: BoxStream&lt;I&gt;) -&gt; BoxStream&lt;Self::Output&gt; {
        use AdbServerRecipeEvent as RecipeEvent;
        use AdbServerRecipeInternalState as RecipeState;

        Box::pin(futures::stream::unfold(
            RecipeState::Init(self.rx),
            |state| async move {
                match state {
                    RecipeState::Init(rx) =&gt; match std::process::Command::new(&quot;adb&quot;)
                        .arg(&quot;shell&quot;)
                        .stdin(std::process::Stdio::piped())
                        .spawn()
                    {
                        Ok(mut data) =&gt; match &amp;data.stdin {
                            Some(_) =&gt; Some((RecipeEvent::Connected, RecipeState::Ready(rx, data))),
                            None =&gt; {
                                warn!(&quot;stdin not found&quot;);
                                data.kill().ok();
                                data.wait().ok();
                                Some((RecipeEvent::Error, RecipeState::Finish))
                            }
                        },
                        // snip.
                    },
                    RecipeState::Ready(mut rx, mut child) =&gt; {
                        loop {
                            if rx.changed().await.is_err() {
                                break;
                            }

                            let data = rx.borrow();
                            debug!(&quot;send data: {}&quot;, data.as_str());

                            // for ignore init value.
                            if data.is_empty() {
                                continue;
                            }

                            let ret = writeln!(child.stdin.as_mut().unwrap(), &quot;{}&quot;, data.as_str());
                            if let Err(e) = ret {
                                warn!(&quot;{:?}&quot;, e);
                                child.kill().ok();
                                child.wait().ok();
                                return Some((RecipeEvent::Error, RecipeState::Disconnecting));
                            }
                        }

                        debug!(&quot;channel closed&quot;);
                        child.kill().ok();
                        child.wait().ok();
                        Some((RecipeEvent::Disconnected, RecipeState::Finish))
                    }
                    RecipeState::Disconnecting =&gt; {
                        Some((RecipeEvent::Disconnected, RecipeState::Finish))
                    }
                    RecipeState::Finish =&gt; {
                        None
                    }
                }
            },
        ))
    }
}

impl Application for Hello {
    fn update(&amp;mut self, message: Self::Message) -&gt; Command&lt;Self::Message&gt; {
        use AppCommand::*;

        match message {
            AdbServerRecipeResult(data) =&gt; match data {
                AdbServerRecipeEvent::Connected =&gt; {
                    info!(&quot;adb connected&quot;);
                    self.adb_connectivity = AdbConnectivity::Connected;
                }
                AdbServerRecipeEvent::Error =&gt; {
                    info!(&quot;some error occurred&quot;);
                }
                AdbServerRecipeEvent::Disconnected =&gt; {
                    info!(&quot;adb disconnected&quot;);
                    self.adb_connectivity = AdbConnectivity::Disconnected;
                    self.adb_server_tx.send(&quot;&quot;.into()).ok();
                }
            },
            // snip.
        }
        // snip.
    }
}
</code></pre><p>Recipe trait の <em>fn hash()</em> は定型文でこのように書いておきます。</p><p>Main thread (Application trait の <em>fn subscription()</em>) から何回も呼ばれるため、ここで違う hash を設定すると以前の Recipe を破棄し新しく Recipe を起動します。逆に言えば 1種類の Subscription を定義して <em>fn subscription()</em> から複数の Subscription を渡しそれぞれ処理をさせたい場合はここでそれぞれ Unique な hash を設定する必要があります。</p><p><em>fn stream()</em> は futures::stream::unfold を使用して少々複雑なことになっていますが戻り値に注目すれば簡単に読めると思います。</p><p>たとえば <code>Some((RecipeEvent::Connected, RecipeState::Ready(rx, data)))</code> のように <em>(A, B)</em> な Tuple を抱えた Option を return していますがこの return した A は Application trait の <em>fn update()</em> に渡され、この iced の仕組みにより Subscription から Main thread に値を渡すことができます。</p><p>return した B は Main thread に値を渡した後、引き続き処理を続行する為に使用する Subscription の状態です。この実装を例にすると</p><ul><li><em>B::Init</em> で Subscription を起動<ul><li>Main thread に <em>A::Connected</em> を送信<ul><li>次に <em>B::Ready</em> に遷移</li></ul></li></ul></li><li><em>B::Ready</em> で adb との接続を確立・ Main thread からの command を受け取り通信<ul><li>rx で受け取った値を adb に送信<ul><li>adb の通信でエラーが発生したら Main thread に <em>A::Error</em> を送信<ul><li>次に <em>B::Disconnecting</em> に遷移</li></ul></li></ul></li><li>Main thread で <code>[ ] login</code> のチェックを外したら <em>A::Disconnedted</em> を送信<ul><li>次に <em>B::Finish</em> に遷移</li></ul></li></ul></li><li><em>B::Disconnecting</em> で切断<ul><li>Main thread に <em>A::Disconnected</em> を送信<ul><li>次に <em>B::Finish</em> に遷移</li></ul></li></ul></li><li><em>B::Finish</em> で None を返すことで Subscription を終了</li></ul><p>といったことをしています。</p><h2>まとめ</h2><p>ちいさい GUI App を作ることができました。</p><p>複数の画面を持つような大きめの App を作る場合は設計を確立する必要がありそう。 Subscription はつらい。</p><p>refs.<br/>
<a href="https://github.com/hecrj/iced">https://github.com/hecrj/iced</a><br/>
<a href="https://github.com/ocornut/imgui">https://github.com/ocornut/imgui</a><br/>
<a href="https://github.com/sukawasatoru/android-commander">https://github.com/sukawasatoru/android-commander</a></p><hr/><p>timestamp<br/>
<!-- -->2021-02-13 (First edition)</p>]]></description>
      <pubDate>Sat Feb 13 2021 00:00:00 GMT+0000 (Coordinated Universal Time)</pubDate>
    </item>
    <item>
      <title>Wasm で Rust と Node.js に同じ実装を使用する</title>
      <link>https://sukawasatoru.com/docs/2021-02-11-wasm</link>
      <guid>https://sukawasatoru.com/docs/2021-02-11-wasm</guid>
      <description><![CDATA[<h1>Wasm で Rust と Node.js に同じ実装を使用する</h1><p>Markdown で書いた文章を GitHub Pages (で使われている Jekyll) で表示していましたが Next.js に興味があったのでそれを使用して HTML に変換するようにしました。また <a href="https://github.com/jameslittle230/stork">Stork</a> を組み込みましたが、一部処理に WebAssembly (wasm) を使える個所があったのでこちらもついでに使ってみました。</p><h2>Markdown から HTML を自動生成する</h2><p>Next.js は <em>getStaticPaths</em> や <em>getStaticProps</em> を使用することで <a href="https://nextjs.org/docs/basic-features/pages#static-generation-recommended">SSG</a> により任意の値を埋め込んだ HTML を生成することができます。この仕組みを利用して Markdown で書いた文章を walk し Parse することで Index を自動生成するようにしました。</p><pre><code class="language-typescript">// src/pages/[stem].tsx

export const getStaticPaths: GetStaticPaths&lt;StaticPath&gt; = async () =&gt; {
  // paths に渡した配列の数だけ HTML を生成することができる。
  const docs = await retrieveDocs();
  const paths: GetStaticPathsResult&lt;StaticPath&gt;[&quot;paths&quot;] = docs.map(value =&gt; ({
    params: {
      stem: value.stem,
    }
  }));

  return {
    paths,
    fallback: false,
  };
};

export const getStaticProps: GetStaticProps&lt;Props, StaticPath&gt; = async context =&gt; {
  // getStaticPaths から渡された context.params を使用して FunctionComponent の
  // props として渡す値を生成する。
  const stem = context.params!.stem;
  const docs = await retrieveDocs();
  const filepath = docs.find(value =&gt; value.stem === stem)!.filepath;
  const matterFile = matter((await readFile(filepath)));
  const mdxSource = await renderToString(matterFile.content,
    {scope: matterFile.data});

  return {
    props: {
      mdxSource,
      stem,
    },
  };
};

// src/function/docs.ts
export const retrieveDocs = async (): Promise&lt;DocEntry[]&gt; =&gt; {
  // src/docs にある markdown を parse する。
  const docsPath = `${process.cwd()}/src/docs`;
  const names = await readdir(docsPath);

  const ret: DocEntry[] = [];

  for (const filename of names) {
    if (!filename.endsWith(&quot;.md&quot;)) {
      continue;
    }

    const filepath = `${docsPath}/${filename}`;
    const entryStat = await stat(`${docsPath}/${filename}`);
    if (!entryStat.isFile()) {
      continue;
    }

    ret.push({
      filepath,
      stem: filename.substring(0, filename.length - &quot;.md&quot;.length),
    });

  }

  return ret;
};
</code></pre><p>このようなコードを書くことで SSG に対応することができます。簡単ですね。</p><h2>Stork で全文検索に対応する</h2><p>Next.js への移行のついでに、興味があったけど使う機会がなかった <a href="https://github.com/jameslittle230/stork">Stork</a> を組み込みました。 Stork は HTML / Markdown を読み込ませることで生成したファイルを JavaScript + wasm に読み込ませることで Server 無しに検索機能を使用することができます。 GitHub Pages を使えば GitHub にファイルを Push するだけで全文検索することができます。</p><pre><code class="language-typescript">const Stork: FunctionComponent&lt;unknown&gt; = () =&gt; {
  useEffect(() =&gt; {
    const waitStork = async () =&gt; {
      const timer = (millis: number) =&gt;
        new Promise(resolve =&gt; window.setTimeout(resolve, millis));
      while (!stork) {
        await timer(100);
      }
    };

    (async () =&gt; {
      await waitStork();
      stork!.register(storkName, &#x27;/blog.st&#x27;, {minimumQueryLength: 2});
    })();
  }, []);

  return &lt;&gt;
    &lt;Head&gt;
      &lt;link rel=&quot;stylesheet&quot; href=&quot;https://files.stork-search.net/basic.css&quot;/&gt;
    &lt;/Head&gt;
    &lt;div className=&quot;stork-wrapper&quot;&gt;
      &lt;input className=&quot;stork-input&quot; data-stork={storkName} placeholder=&quot;Search...&quot;/&gt;
      &lt;div className=&quot;stork-progress&quot; style={{width: &#x27;100%&#x27;, opacity: 0}}&gt;&lt;/div&gt;
      &lt;div className=&quot;stork-output&quot; data-stork={`${storkName}-output`}&gt;&lt;/div&gt;
    &lt;/div&gt;
    &lt;script src=&quot;https://files.stork-search.net/stork.js&quot;&gt;&lt;/script&gt;
  &lt;/&gt;;
};
</code></pre><p>React の使い方としてあっているか分かりませんがやっていることは特定の className を設定した div を用意して css と javascript を読み込み CLI から生成したファイルを <code>stork.register()</code> で読み込ませているだけです。</p><p>これでめでたしめでたし、で終われば良かったのですが、 Stork の CLI でファイルを生成するためには検索対象のすべてのページそれぞれに設定を記述する必要があります。 <a href="https://github.com/jameslittle230/stork#building-your-own-index">README のサンプルの設定</a> を一部引用すると</p><pre><code class="language-toml">[input]
files = [
    {path = &quot;federalist-1.txt&quot;, url = &quot;/federalist-1/&quot;, title = &quot;Introduction&quot;},
    {path = &quot;federalist-2.txt&quot;, url = &quot;/federalist-2/&quot;, title = &quot;Concerning Dangers from Foreign Force and Influence&quot;},
      # snip.
    {path = &quot;federalist-9.txt&quot;, url = &quot;/federalist-9/&quot;, title = &quot;The Union as a Safeguard Against Domestic Faction and Insurrection&quot;},
    {path = &quot;federalist-10.txt&quot;, url = &quot;/federalist-10/&quot;, title = &quot;The Union as a Safeguard Against Domestic Faction and Insurrection 2&quot;}
]
</code></pre><p>といった感じですので Markdown ファイルを作成するたびに同期する必要があるので、できれば避けたいですね。</p><p>幸い Stork は Rust で実装されているため Rust で CLI を作成し設定ファイルを walk する実装をすることでこの手間は解決することができます。ある程度 Rust を書いている人はこの toml を見て「多分 toml-rs 使っているから struct 作ってやればいいんじゃないかな」と思うかもしれませんが、その通りで walk する crate とかを使ってあとは <em>.md</em> のファイルを Vec にすればほぼやりたいことは実現できます。</p><p>これでやりたいことができてめでたしめでたし、と思いましたがこのあと Node.js で書いた Index を生成する実装に機能追加していくと Rust で書いた Stork 向けの設定ファイルを生成する実装と同じことをしていることに気付きました。</p><p>今後変更を加える場合 Node.js / Rust それぞれに同じ変更をしなければならないため、ミスによりそれぞれの動きに差分を出してしまうことは避けたいです。</p><p>せめて Markdown を Parse する部分の実装だけでも共通化できたら、、、</p><h2>WebAssembly で Rust と Node.js で処理を共通化する</h2><p>ところで Rust は wasm に対応しています。ということは Rust で実装すればもちろん Rust に使えますし Rust から wasm で出力すれば Node.js に読み込ませて実行することができます。最高。</p><p>前置きが長くなりましたがここから wasm の環境構築から実際に動くものを作るまでをやっていきます。</p><p>実装したものは <a href="https://github.com/sukawasatoru/blog/tree/master/tools/docs-parser">GitHub: sukawasatoru/blog</a> に push しました。</p><p><a href="https://github.com/rustwasm/wasm-bindgen">wasm-bindgen</a> という有名な crate があるので、初手それのみを使用して実装しましたがいろいろ気をつけなければならない点があり面倒でしたので (Node.js に load させるには、あたりまで進めてあきらめた) wasn-bindgen に加えいろいろ気を使ってくれるツールの <a href="https://github.com/rustwasm/wasm-pack">wasm-pack</a> を使用します。</p><p>wasm-pack を使うに当たって覚えることは</p><ol><li><a href="https://github.com/rustwasm/wasm-pack/releases">Releases</a> から Download するもしくは <code>cargo install</code> で Install する</li><li>通常の Project で <code>wasm-pack build</code> を実行して Build する</li></ol><p>の 2点です。しばらくはこれだけで十分開発できます。</p><p>ちなみに Guide を読むと <code>wasm-pack build</code> の Option に <code>--target</code> があり <em>bundler</em> / <em>nodejs</em> / <em>web</em> / <em>no-module</em> を指定できるので nodejs を使おうと思うかもしれませんが、今回は Next.js (内部で Webpack) で開発しているので bundler を使う必要があります。</p><p><a href="https://rustwasm.github.io/docs/wasm-bindgen/">wasm-bindgen の Guide</a> と <a href="https://rustwasm.github.io/wasm-pack/book/">wasn-pack の Guide</a> を一通り読むと次の Cargo.toml と lib.rs をベースにすればよさそうでした。</p><pre><code class="language-toml">[lib]
crate-type = [&quot;cdylib&quot;, &quot;rlib&quot;]

[features]
wasm = []

[dependencies.wasm-bindgen]
version = &quot;=0.2.70&quot;
features = [&quot;serde-serialize&quot;]

[dev-dependencies]
wasm-bindgen-test = &quot;=0.3.20&quot;

[profile.release]
opt-level = &quot;s&quot;
lto = true
</code></pre><pre><code class="language-rust">use wasm_bindgen::prelude::*;

#[wasm_bindgen]
extern &quot;C&quot; {
    #[wasm_bindgen(js_namespace = console)]
    fn log(s: &amp;str);
}

macro_rules! console_log {
    ($($t:tt)*) =&gt; {
        if cfg!(feature=&quot;wasm&quot;) {
            log(&amp;format_args!($($t)*).to_string())
        } else {
            // ここでは tracing を使っているが env_logger など
            // 好みの log crate を使って良い。
            #[cfg(not(feature=&quot;wasm&quot;))]
            tracing::info!($($t)*)
        }
    }
}

#[wasm_bindgen]
pub fn greet() {
    console_log!(&quot;Hello {}&quot;, &quot;world&quot;);
}
</code></pre><p>簡単な説明をすると <code>extern &quot;C&quot;</code> は Rust から使用する Node.js 側の IF を記述します。この例では Rust から <code>console.log()</code> を実行します。</p><p><code>macro_rules! console_log</code> は log を wrap したものです。 CLI 向けと Node.js の両方で log を出力するために features を使って log の向きを変えるよう wrap しています。</p><p>この例ですと <code>wasm-pack build -- --features wasm</code> で Build することで <code>console_log!(&quot;Helllo {}, &quot;world&quot;)</code> としたときに <code>console.log(&quot;Hello world&quot;)</code> が実行され <code>cargo build</code> で Build することで <code>tracing::info!(&quot;Hello world&quot;)</code> が実行されます。</p><p><code>#[wasm_bindgen] pub fn greet()</code> は Rust から公開する関数です。 Node.js からは</p><pre><code class="language-typescript">// foo は wasm-pack が生成する。
const {greet} = await import(&quot;foo&quot;);
greet();
</code></pre><p>とすることで使用できます。 ここで 1点ルールが有り wasm-pack が生成した JS は Dynamic import で Import する必要があります。</p><p>ではここから Rust / Node.js 共通で利用したい関数を実装していきます。今回は Markdown のフォーマットの String からタイトル・作成日時・更新日時を Parse する関数を作成します。重要な部分を抜き出すと次のような Code になります:</p><pre><code class="language-rust">#[wasm_bindgen]
#[derive(serde::Serialize)]
pub struct DocEntry {
    // 文章のタイトル。
    title: String,
    // 文章の作成日時。
    first_edition: String,
    // 文章の更新日時。
    last_modify: Option&lt;String&gt;,
}

#[wasm_bindgen]
impl DocEntry {
    #[wasm_bindgen(getter)]
    pub fn title(&amp;self) -&gt; String {
        self.title.to_string()
    }

    #[wasm_bindgen(getter = firstEdition)]
    pub fn first_edition(&amp;self) -&gt; String {
        self.first_edition.to_string()
    }

    #[wasm_bindgen(getter = lastModify)]
    pub fn last_modify(&amp;self) -&gt; Option&lt;String&gt; {
        self.last_modify.as_ref().map(|data| data.to_string())
    }
}

#[wasm_bindgen(js_name = parseDocs)]
pub fn parse_docs(doc: &amp;str) -&gt; Option&lt;DocEntry&gt; {
    console_log!(&quot;parse_docs&quot;);

    let mut title = None;
    let mut first_edition = None;
    let mut last_modify = None;
    let first_regex = regex::Regex::new(r#&quot;^([-0-9]*) \(First edition\)&quot;#).unwrap();
    let modify_regex = regex::Regex::new(r#&quot;^([-0-9]*) \(Last modify\)&quot;#).unwrap();
    let title_sep_regex = regex::Regex::new(r#&quot;^=*$&quot;#).unwrap();

    for entry in doc.lines() {
        // parse content.
        // snip.
        match chrono::NaiveDate::from_str(&amp;data[1]) {
            Some(data) =&gt; {}
            None =&gt; {}
        }
        // snip.
    }

    if title.is_none() {
        console_log!(&quot;the title is None&quot;);
        return None;
    }

    if first_edition.is_none() {
        console_log!(&quot;the first_efition is None&quot;);
        return None;
    }

    Some(DocEntry {
        title: title.unwrap(),
        first_edition: first_edition.unwrap().to_string(),
        last_modify: last_modify.map(|data| data.to_string()),
    })
}
</code></pre><p><code>pub fn parse_docs()</code> が Rust と Node.js に公開する関数です。この関数は日時のために chrono carte とタイトルや日付を抜き出すために regexp crate を使用していますが wasm で出力する場合でも普段使用している crate を使用することができます。</p><p>この関数の戻り値に struct を使用していますが Public な <code>#[wasm_bindgen]</code> Attribte のある struct は Node.js で使用することができます。使用する struct にはルールがあり Public な field は Copy 可能でなければなりません。今回は (memory leak とか慣習とか正しいか分かりませんが) <code>#[wasm_bindgen(getter)]</code> のある method を実装することで Copy 可能のルールを回避しました。</p><p>手っ取り早く戻り値に scruct を使いたい場合は <code>#[wasm_bindgen]</code> とか全く考慮しない struct を</p><pre><code class="language-rust">JsValue::from_serde(&amp;value).unwrap()
</code></pre><p>とすることができます。 struct で返す場合と JsValue で返す場合の大きな違いとしては <code>wasm-pack build</code> したときに TypeScript の型が生成されるのですが、関数の戻り値が明示的な型になるか any になるかがあります。面倒なので全部 JsValue で済ませたいですがこれは悩みますね、、、</p><h2>まとめ</h2><p>wasm を使用して Rust と Node.js の実装を共通化することができました。</p><p>refs.<br/>
<a href="https://github.com/jameslittle230/stork">https://github.com/jameslittle230/stork</a><br/>
<a href="https://nextjs.org/docs">https://nextjs.org/docs</a><br/>
<a href="https://rustwasm.github.io/docs/wasm-bindgen/">https://rustwasm.github.io/docs/wasm-bindgen/</a><br/>
<a href="https://rustwasm.github.io/wasm-pack/book/">https://rustwasm.github.io/wasm-pack/book/</a><br/>
<a href="https://github.com/sukawasatoru/blog/tree/master/tools/docs-parser">https://github.com/sukawasatoru/blog/tree/master/tools/docs-parser</a>  </p><hr/><p>timestamp<br/>
<!-- -->2021-02-11 (First edition)</p>]]></description>
      <pubDate>Thu Feb 11 2021 00:00:00 GMT+0000 (Coordinated Universal Time)</pubDate>
    </item>
    <item>
      <title>SonarQube</title>
      <link>https://sukawasatoru.com/docs/sonarqube</link>
      <guid>https://sukawasatoru.com/docs/sonarqube</guid>
      <description><![CDATA[<h1>SonarQube</h1><p>まず -Dsonar.branch.name=jenkinsfile を指定せずに build する必要がある
--debug を行ったときに
sonarcloud で project が作成されていないときに sonar.branch.name を指定すると --debug を付けると以下のエラーを確認できる
<!-- -->[DEBUG][org.sonarqube.gradle.SonarQubeTask]<!-- --> GET 404 <a href="https://sonarcloud.io/api/project_branches/list?project=android-tinytable">https://sonarcloud.io/api/project_branches/list?project=android-tinytable</a></p><p>[ERROR][org.gradle.internal.buildevents.BuildExceptionReporter]<!-- --> com/android/build/gradle/api/BaseVariant が発生するとき
sonarqube から :sonarqube に変更すると通る
原因は不明
-&gt; 解析されていないだけなのでこれは使用することは出来ない</p><p>./gradlew --debug -Dsonar.organization=sukawasatoru-github -Dsonar.host.url=<a href="https://sonarcloud.io">https://sonarcloud.io</a> -Dsonar.login=31ec2b9759cb606d07b14ab4d61c1a38e6cfa4c5 -Dsonar.projectName=android-tinytable -Dsonar.projectKey=android-tinytable -Dsonar.verbose=true :onarqube</p><hr/><p>timestamp<br/>
<!-- -->2018-02-10 (First edition)</p>]]></description>
      <pubDate>Sat Feb 10 2018 00:00:00 GMT+0000 (Coordinated Universal Time)</pubDate>
    </item>
    <item>
      <title>Surface Pro 3 Recovery</title>
      <link>https://sukawasatoru.com/docs/surfacepro3-recovery</link>
      <guid>https://sukawasatoru.com/docs/surfacepro3-recovery</guid>
      <description><![CDATA[<h1>Surface Pro 3 Recovery</h1><p>Surface Pro 3 を復旧するメモ</p><h2>Suraface Pro 3 からユーティリティを使用して USB に回復ディスクを作ってはいけない</h2><p>通常の PC で USB メモリなど外部ストレージを使用し復旧する際はその PC で作成した回復ディスクを使用して復旧する。</p><p>ただし Surface Pro 3 ではこの方法で復旧することが出来ない。復旧中に SSD のパーティションを書き換えている段階あたりで必ず 問題が発生しました 的なメッセージが表示され失敗されてしまう。</p><h2>Surface Pro 3 の回復ディスクを作成する</h2><p>ユーティリティを使用せずに回復ディスクを作成すれば問題ない。<br/>
<!-- -->作成にはどの PC を利用しても良い</p><p>回復ディスクを作成するためまず
<a href="https://support.microsoft.com/surfacerecoveryimage">Download a recovery image for your Surface</a>
からイメージをダウンロードする。なおダウンロードするためには予め Surface Pro 3 を自分の Live ID に登録する必要があるため、まだ登録していない場合は登録する。<br/>
<!-- -->イメージは zip でアーカイブされているのでダウンロードが完了したらそれを展開する。
Boot や EFI という名前のディレクトリやファイルが展開される。</p><p>次に回復ディスクを作成するために USB メモリをフォーマットする。<br/>
<!-- -->使用するディスクは 8GB - 64GB のものを使用し FAT32 でフォーマットする。
回復ディスクを作成するために最低 8GB が必要。FAT32 でフォーマットできれば恐らく 128GB のメモリを使用しても良いが試していない。</p><p>フォーマットが完了したら zip を展開したファイル (Boot や EFI など) をすべて USB メモリの root にコピーする。</p><h2>回復する</h2><p>通常の方法で。</p><p>ref.<br/>
<a href="https://support.microsoft.com/surfacerecoveryimage">Download a recovery image for your Surface</a><br/>
<a href="https://support.microsoft.com/help/4023512/surface-creating-and-using-a-usb-recovery-drive">Creating and using a USB recovery drive</a></p><hr/><p>timestamp<br/>
<!-- -->2018-01-21 (First edition)</p>]]></description>
      <pubDate>Sun Jan 21 2018 00:00:00 GMT+0000 (Coordinated Universal Time)</pubDate>
    </item>
    <item>
      <title>Jenkins</title>
      <link>https://sukawasatoru.com/docs/jenkins</link>
      <guid>https://sukawasatoru.com/docs/jenkins</guid>
      <description><![CDATA[<h1>Jenkins</h1><p>GitHub のために余っている MacBook Pro に Ubuntu と Jenkins Server と Android を build するための Slave を setup したときのメモ</p><ul><li><a href="#os-%E3%81%AE-install">OS の Install</a></li><li><a href="#jenkins-%E3%81%AE-install-%E5%89%8D%E3%81%AE%E6%BA%96%E5%82%99">Jenkins の Install 前の準備</a></li><li><a href="#jenkins-%E3%81%AE-install">Jenkins の Install</a></li><li><a href="#nginx-%E3%81%AE-install">NGINX の Install</a></li><li><a href="#jenkins-%E3%81%AE-setup">Jenkins の setup</a></li><li><a href="#slave-%E3%81%AE-setup">Slave の setup</a></li><li><a href="#jenkins-%E3%81%AE-slave-%E3%81%AE%E8%A8%AD%E5%AE%9A">Jenkins の Slave の設定</a></li><li><a href="#github-personal-access-token-%E3%81%AE%E8%A8%AD%E5%AE%9A">GitHub personal access token の設定</a></li><li><a href="#build-%E7%A2%BA%E8%AA%8D">Build 確認</a></li><li><a href="#gradleproperties-%E3%81%AE%E8%A8%AD%E5%AE%9A">gradle.properties の設定</a></li><li><a href="#netdata-%E3%81%AE-install-%E3%81%A8-nginx-%E3%81%AE%E8%A8%AD%E5%AE%9A">netdata の Install と NGINX の設定</a></li></ul><h2>OS の Install</h2><p>Ubuntu のサイト <a href="https://www.ubuntu.com/">The leading operating system for PCs, IoT devices, servers and the cloud <!-- -->|<!-- --> Ubuntu</a> から Server 版 Image を Download する。</p><p>version は一般的には LTS で良いと思う。OS を設定するのが好きとか Upgrade の手間を惜しまないのであれば LTS とか気にせず最新のでも良い。</p><p>今回は mac 上の Parallels に Install するので新規に VM を作成する。<br/>
<!-- -->Ubuntu だと Parallels の express instllation 機能を使用できるが今回は Ubuntu の Installer を使用した。<br/>
<!-- -->Memory は 1GB を設定した。その他の VM の設定は Default で問題ないが mac との Application の共有や mount や不要な Device を削除した。
一通り設定したら IP Address を固定することで作業がしやすくなるので MAC Address をメモして Router (DHCP Server) の IP を固定する設定をする。<br/>
<!-- -->設定を完了すると Installer が起動する。ここからは特に特別な設定をせず進めた。</p><h2>Jenkins の Install 前の準備</h2><p>OS の Install が終わって login 画面まで進んだら login し Jenkins と NGINX を設定する前に作業しやすいように最低限の変更を行った</p><pre><code class="language-bash"># visudo のために default の editor を変更
sudo update-alternatives --config editor

# visudo で NOPASSWD の設定
# visudo を実行したら下の方に以下を設定
# &lt;user 名&gt; ALL=(ALL) NOPASSWD: ALL
sudo visudo

# 更新
sudo apt update
sudo apt upgrade
sudo apt upgrade
sudo apt dist-upgrade
sudo shutdown -r now

# peco と history を組み合わせて操作したいので peco の Install
sudo apt install peco

# 環境変数や History や peco を使いやすくするための key assigin を設定
vim .profile
vim .bashrc
vim .inputrc

# 変更を簡単に反映するために logout して login
# ...

# mac から作業したいので鍵の生成
ssh-keygen -t ed25591 -C &quot;hoge&quot;

mkdir .ssh
chmod 700 .ssh
cat hoge.pub &gt; .ssh/authorized_keys
chmod 644 .ssh/authorized_keys

# mosh 経由で接続したいので mosh の Install
sudo apt install mosh

# mac から接続・~/.ssh/config 設定のために ip の確認
ip a
</code></pre><p>作成した鍵は mac から scp で秘密鍵を転送する。</p><h2>Jenkins の Install</h2><p>Document の通りに Install する</p><p><a href="https://jenkins.io/">Jenkins</a><br/>
<a href="https://pkg.jenkins.io/debian/">Debian Repository for Jenkins</a></p><pre><code class="language-bash"># Jenkins の為に JVM を Install
sudo apt install openjdk-8-jre-headless

wget -q -O - https://pkg.jenkins.io/debian/jenkins.io.key | sudo apt-key add -

# 以下を追加
# deb https://pkg.jenkins.io/debian binary/
sudo vim /etc/apt/sources.list

sudo apt update
sudo apt install jenkins
</code></pre><p>Default の設定だと <code>http://&lt;hostname&gt;:8080/ </code>が URI になる。
これを <code>http://&lt;hostname&gt;:8080/jenkins</code> に変更したいので <code>/etc/default/jenkins</code> の一番下にある <code>JENKINS_ARGS</code> に <code>--prefix=$PREFIX</code> を追加する。<br/>
<code>JENKINS_ARGS=&quot;--webroot=/var/cache/$NAME/war --httpPort=$HTTP_PORT --prefix=$PREFIX&quot;</code><br/>
<!-- -->ちなみにこの file は <code>apt upgrade</code> で更新すると更新中に上書きをするか聞かれることがあるのでそのときまっさらな状態に上書きしないように注意する。</p><p>追加したら <code>sudo systemctl restart jenkins</code> で設定を反映させる。</p><p>Firewall が有効になっていないので <code>http://&lt;ip&gt;:8080/jenkins</code> から確認することが出来る。
確認できたら設定を進めず NGINX の Install を先に行う</p><h2>NGINX の Install</h2><p><code>apt install nginx</code> で Install を行う</p><p>Install 後 NGINX を経由して Jenkins を使用できるよう設定を変更する。
Ubuntu の NGINX は <code>/etc/nginx/sites-available</code> に設定を書き <code>/etc/nginx/sites-enable</code> に Symbolic link を張ることで設定を行う。
設定は今のところは Document <a href="https://wiki.jenkins.io/display/JENKINS/Jenkins+behind+an+NGinX+reverse+proxy">Jenkins behind an NGinX reverse proxy - Jenkins - Jenkins Wiki</a> に書いてある通りで良い。<br/>
<!-- -->設定を行ったら <code>sudo nginx -t</code> で問題ないか確認し <code>sudo systemctl reload nginx</code> で設定を反映させる。
問題なく設定できると <code>http://&lt;ip&gt;/jenkins</code> で確認できる。</p><h2>Jenkins の setup</h2><p>NGINX で設定した URI から画面通りに進める</p><p>途中で Install する Plugin を聞かれるがこの段階では全部チェックを外してしまって良い。
設定が完了するとお馴染みの top page に遷移する。この後お好みで自分の user account を作成しても良いしこのまま admin account で操作しても良い。</p><p>GitHub の Repository を Polling して Build を行いたいため必要な Plugin を Install する。
<em>Manage Jenkins</em> -&gt; <em>Manage Plugins</em> から以下の Plugin を Install した。</p><table><thead><tr><th align="left">name</th><th align="left">description</th></tr></thead><tbody><tr><td align="left">Pipeline</td><td align="left">Jenkinsfile で Build するために使用 <a href="https://plugins.jenkins.io/workflow-aggregator">https://plugins.jenkins.io/workflow-aggregator</a></td></tr><tr><td align="left">Pipeline: GitHub</td><td align="left">GitHub から Build するために使用 <a href="https://wiki.jenkins.io/display/JENKINS/Pipeline+Github+Plugin">https://wiki.jenkins.io/display/JENKINS/Pipeline+Github+Plugin</a></td></tr><tr><td align="left">GitHub Organization Folder Plugin</td><td align="left">GitHub の account にある Repository を polling するために使用。ある特定の Repository だけ polling したいのであれば不要。 <a href="https://plugins.jenkins.io/github-organization-folder">https://plugins.jenkins.io/github-organization-folder</a></td></tr><tr><td align="left">Mask Passwords Plugin</td><td align="left">Jenkinsfile で Credential を使用するなど Output console に出力される token を mask するために使用 <a href="https://plugins.jenkins.io/mask-passwords">https://plugins.jenkins.io/mask-passwords</a></td></tr><tr><td align="left">SSH Slaves</td><td align="left">Slave を簡単に設定するために使用 <a href="https://plugins.jenkins.io/ssh-slaves">https://plugins.jenkins.io/ssh-slaves </a></td></tr></tbody></table><h2>Slave の setup</h2><p>Android の Build に使用する Slave を setup する。
OS の Install や setup の前準備は上記と同じで良い。</p><p>どちらも終わったら Android SDK の setup を行う。
Android SDK を使用するためには JDK の Install と build-tools で使用する package を apt で Install する必要があるため Document <a href="https://source.android.com/setup/initializing">Establishing a Build Environment <!-- --> <!-- -->|<!-- --> <!-- --> Android Open Source Project</a> を参考に設定する。</p><pre><code class="language-bash">sudo apt install openjdk-8-jdk-headless
sudo apt install git-core gnupg flex bison gperf build-essential zip curl zlib1g-dev gcc-multilib g++-multilib libc6-dev-i386 lib32ncurses5-dev x11proto-core-dev libx11-dev lib32z-dev ccache libgl1-mesa-dev libxml2-utils xsltproc unzip
</code></pre><p>次に Android SDK の download と setup を行う。<br/>
<!-- -->Android SDK は <a href="https://developer.android.com/studio/index.html#downloads">Download Android Studio and SDK Tools | Android Studio</a> から最新の SDK を Download する。
Download した SDK は /opt に展開して使用することにする。
展開後は自分の環境で Build に必要な package を Install する</p><pre><code class="language-bash">curl -LO https://dl.google.com/android/repository/sdk-tools-linux-3859397.zip
unzip -d android-sdk-linux sdk-tools-linux-3859397.zip
sudo mv android-sdk-linux/ /opt/

# 既存の package を更新
/opt/android-sdk-linux/tools/bin/sdkmanager --update

# 必要な package を確認
/opt/android-sdk-linux/tools/bin/sdkmanager --list

# package の Install
# 使用する Version の build-tools と platforms を指定する
# Gradle v4.1 以降の環境であれば extras は特に必要無い
/opt/android-sdk-linux/tools/bin/sdkmanager &#x27;build-tools;27.0.3&#x27; &#x27;platforms;android-27&#x27; &#x27;platforms;android-26&#x27; &#x27;platforms;android-25&#x27; &#x27;platforms;android-24&#x27; &#x27;platforms;android-23&#x27; &#x27;platforms;android-22&#x27; &#x27;platforms;android-21&#x27;
</code></pre><p>Android SDK の setup が完了したら次に Jenkins の node として使用する user account と Jenkins から SSH 経由で接続するための鍵を生成する。
鍵は今のところは ed_25519 ではなく RSA を使用するのがトラブル無く良さそう。</p><pre><code class="language-bash">sudo adduser jenkinsbuild
sudo su jenkinsbuild

# ここから jenkinsbuild
ssh-keygen -b 4096 -t rsa -C &quot;ubuntu jenkins&quot;

mkdir .ssh
chmod 700 .ssh/
cat id_rsa-ubuntu-jenkins.pub &gt; .ssh/authorized_keys
chmod 644 .ssh/authorized_keys
</code></pre><h2>Jenkins の Slave の設定</h2><p>秘密鍵は Jenkins の <em>Credentials</em> に Copy Paste を行い設定する</p><p>Credentials に登録したらそれを用いて Slave を登録する。
<em>Manage Jenkins</em> -&gt; <em>Manage Nodes</em> から <em>New Node</em> で設定を行う。</p><p><img src="/jenkins-slave.png" alt="jenkins-slave.png"/></p><p><em>Remote root directory</em> は Build 用に作成した user の home directory を指定した。<br/>
<em>Labels</em> は Jenkinsfile から使用する Slave を設定するために付けた。<br/>
<em>Launch method</em> の <em>Host Key Verification Strategy</em> は <em>Non verifying Verification Strategy</em> を設定した。<br/>
<em>Node Properties</em> の <em>Environment variables</em> から Android SDK に必要な環境変数を <a href="https://source.android.com/setup/initializing">Establishing a Build Environment <!-- --> <!-- -->|<!-- --> <!-- --> Android Open Source Project</a> の Document を参照して定義した。</p><h2>GitHub personal access token の設定</h2><p>GitHub の Repository を pull、Build Status の更新をするために access token を設定する</p><p>GitHub の <em>Settings</em> から <em>Developer settings</em> -&gt; <em>Personal access tokens</em> を選択し <em>Generate new token</em> の Button を押す。
必要は権限は <em>Repo</em> の <em>Full control of private repositories</em> だけでよい。private repository を使用していない場合など必要に応じてさらに権限を絞っても良い。
生成した token は Jenkins の <em>Credentials</em> から <em>Add Credentials</em> を行う。</p><p><em>Kind</em> は Username with password を設定する。<br/>
<em>Username</em> は空欄にする。<br/>
<em>Password</em> に生成した token を設定する。<br/>
<em>Description</em> は分かりやすい適当な値を設定する。<br/>
<em>ID</em> は Jenkinsfile で使用する値である。お好みで自分で設定してもよいし空欄にして自動生成させるのも良い。</p><h2>Build 確認</h2><p>Build できるまで設定できたので実際に Build を行い設定してみる</p><p>Jenkins の <em>New Item</em> から <em>GitHub Organization</em> を選択する。<br/>
<em>GitHub Organization</em> の <em>Credentials</em> には GitHub で生成した token を指定し <em>owner</em> を適切に設定する。<br/>
<!-- -->そのほかお好みで <em>Scan Organization Triggers</em> の <em>Periodically if not otherwise run</em> の <em>Interval</em> を設定する。<br/>
<!-- -->その他は Default で特に問題ない。</p><p>設定すると <em>owner</em> で設定した account の Repository の scan が始まる。
Repository の root に Jenkinsfile が格納されていれば Jenkins にその Repository と Branch が登録され Build が始まる。</p><p>自分は test 用の repository を作成し以下の Jenkinsfile を書いた。</p><pre><code class="language-jenkinsfile">pipeline {
    agent { label &#x27;android&#x27; }

    stages {
        stage(&#x27;Build&#x27;) {
            steps {
                sh &#x27;./gradlew --info --profile --no-daemon build&#x27;
            }
        }

        stage(&#x27;Report&#x27;) {
            steps {
                withCredentials([string(credentialsId: &#x27;5f36d99d-2541-46a5-9ee1-94a9e2ea338e&#x27;, variable: &#x27;SONAR_LOGIN&#x27;)]) {
                    sh &#x27;./gradlew --info --no-daemon -Dsonar.organization=sukawasatoru-github -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=$SONAR_LOGIN sonarqube&#x27;
                }
            }
        }
    }
}
</code></pre><p>適切に設定できていればエラー無く Build 出来たことを Jenkins と GitHub の commit status から確認できる。</p><h2>gradle.properties の設定</h2><p>Slave に Build 用に作成した account の <code>$HOME/.gradle/gradle.properties</code> に gradle の設定を行う	</p><p>no-daemon と android cache の設定は恐らく必要でそのほか JVM の memory の設定も行う。</p><pre><code class="language-properties">org.gradle.daemon=false
android.enableBuildCache=false
</code></pre><h2>netdata の Install と NGINX の設定</h2><p>Jenkins の VM と Slave の VM の状態を Browser から確認したいので <a href="https://github.com/firehol/netdata">netdata</a> の Install を行う。
Install 方法は色々あるが Build されたものを <a href="https://github.com/firehol/netdata/wiki/Installation">Installation · firehol/netdata Wiki</a> を参考に Install する。</p><pre><code class="language-bash">bash &lt;(curl -Ss https://my-netdata.io/kickstart-static64.sh)
</code></pre><p>Install できたら 2つの netdata を Jenkins で使用している NGINX から参照できるよう設定する。
設定は Document <a href="https://github.com/firehol/netdata/wiki/Running-behind-nginx#as-a-subfolder-for-multiple-netdata-servers-via-one-nginx">Running behind nginx · firehol/netdata Wiki</a> を参照して行う。ほぼ書かれているとおりに行って問題ない。</p><p>refs.<br/>
<a href="https://www.ubuntu.com/">The leading operating system for PCs, IoT devices, servers and the cloud | Ubuntu</a><br/>
<a href="https://jenkins.io/">Jenkins</a><br/>
<a href="https://pkg.jenkins.io/debian/">Debian Repository for Jenkins</a><br/>
<a href="https://wiki.jenkins.io/display/JENKINS/Jenkins+behind+an+NGinX+reverse+proxy">Jenkins behind an NGinX reverse proxy - Jenkins - Jenkins Wiki</a><br/>
<a href="https://plugins.jenkins.io/workflow-aggregator">Pipeline</a><br/>
<a href="https://wiki.jenkins.io/display/JENKINS/Pipeline+Github+Plugin">Pipeline Github Plugin - Jenkins - Jenkins Wiki</a><br/>
<a href="https://plugins.jenkins.io/github-organization-folder">GitHub Organization Folder</a><br/>
<a href="https://plugins.jenkins.io/mask-passwords">Mask Passwords</a><br/>
<a href="https://plugins.jenkins.io/ssh-slaves">SSH Slaves</a><br/>
<a href="https://source.android.com/setup/initializing">Establishing a Build Environment <!-- --> <!-- -->|<!-- --> <!-- --> Android Open Source Project</a><br/>
<a href="https://developer.android.com/studio/index.html#downloads">Download Android Studio and SDK Tools | Android Studio</a><br/>
<a href="https://source.android.com/setup/initializing">Establishing a Build Environment <!-- --> <!-- -->|<!-- --> <!-- --> Android Open Source Project</a><br/>
<a href="https://github.com/firehol/netdata">firehol/netdata: Get control of your servers. Simple. Effective. Awesome! https://my-netdata.io/</a><br/>
<a href="https://github.com/firehol/netdata/wiki/Installation">Installation · firehol/netdata Wiki</a><br/>
<a href="https://github.com/firehol/netdata/wiki/Running-behind-nginx#as-a-subfolder-for-multiple-netdata-servers-via-one-nginx">Running behind nginx · firehol/netdata Wiki</a></p><hr/><p>timestamp<br/>
<!-- -->2017-12-24 (First edition)<br/>
<!-- -->2020-02-07 (Last modify)</p>]]></description>
      <pubDate>Sun Dec 24 2017 00:00:00 GMT+0000 (Coordinated Universal Time)</pubDate>
    </item>
    <item>
      <title>Android emulator</title>
      <link>https://sukawasatoru.com/docs/android-emulator</link>
      <guid>https://sukawasatoru.com/docs/android-emulator</guid>
      <description><![CDATA[<h1>Android emulator</h1><p>avdmanager で avd を作成したメモ</p><h2>avd の作成</h2><p><code>avdmanager create avd</code> で help を確認できるので、基本その通りに実行すれば問題ない。</p><pre><code class="language-sh">avdmanager -v create avd --sdcard 200M --tag android-tv --package &#x27;system-images;android-26;android-tv;x86&#x27; --name 26tv-x86 --abi x86 --device tv_1080p
</code></pre><table><thead><tr><th align="left">arg</th><th align="left">description</th></tr></thead><tbody><tr><td align="left">sdcard</td><td align="left">path またはサイズを入力する。サイズの単位は K または M (c m ではダメ)。</td></tr><tr><td align="left">tag</td><td align="left"><code>avdmanager list device</code> の <code>Tag</code></td></tr><tr><td align="left">package</td><td align="left"><code>sdkmanager --list</code> の <code>Path</code>。見切れる場合は <code>--verbose</code> を付ける。</td></tr><tr><td align="left">name</td><td align="left">お好きな名前</td></tr><tr><td align="left">abi</td><td align="left">abi。jni 使っていない限り <code>x86</code> で困ったことはない。</td></tr><tr><td align="left">device</td><td align="left"><code>avdmanager list device</code> の <code>id</code>。数字でも text でもよい</td></tr></tbody></table><h2>avd のカスタマイズ</h2><p>$HOME/.android/avd 以下に <code>&lt;name&gt;.avd</code> の directory が作成される。
カスタマイズはその中の config.ini を編集する。hardware-qemu.ini は emulator を起動する際に自動的に書き換えられるファイルのため自分で書き換える必要は無い。
Android TV の avd を動かすために追加で以下の設定を行った。</p><table><thead><tr><th align="left">name</th><th align="left">description</th></tr></thead><tbody><tr><td align="left">hw.gpu.enabled</td><td align="left">true にしないと真っ黒</td></tr><tr><td align="left">hw.ramSize</td><td align="left">恐らく必須ではないがお好みで</td></tr><tr><td align="left">skin.*</td><td align="left">正しく設定しないと TV なのに縦長の画面になる</td></tr><tr><td align="left">vm.heapSize</td><td align="left">恐らく必須ではないがお好みで</td></tr></tbody></table><pre><code class="language-diff">@@ -10,15 +10,21 @@
 hw.device.manufacturer=Google
 hw.device.name=tv_1080p
 hw.gps=yes
+hw.gpu.enabled=yes
 hw.keyboard=yes
 hw.keyboard.lid=yes
 hw.lcd.density=320
 hw.mainKeys=yes
+hw.ramSize=2048
 hw.sdCard=yes
 hw.sensors.orientation=no
 hw.sensors.proximity=no
 hw.trackBall=no
 image.sysdir.1=system-images/android-26/android-tv/x86/
 sdcard.size=200M
+skin.dynamic=no
+skin.name=1920x1080
+skin.path=1920x1080
 tag.display=Android TV
 tag.id=android-tv
+vm.heapSize=192
</code></pre><h2>HAXM の install (macOS)</h2><p>sdkmanager の <code>extras;intel;Hardware_Accelerated_Execution_Manager</code> で download できる HAXM を install する。</p><p>$ANDROID_SDK_ROOT/extras/intel/Hardware_Accelerated_Execution_Manager に download されるこれを CLI から install や uninstall するためにはまず実行権限を付与する必要がある。</p><p><code>chmod +x HAXM\ installation silent_install.sh</code></p><p>そのあと <code>HAXM\ installation</code> で install できる。</p><p>help は <code>HAXM\ installation -h</code> または <code>silent_install.sh -h</code> で確認できる。</p><h2>emulator の起動</h2><p><code>emulator -avd &lt;name&gt;</code> で起動する。<br/>
<!-- -->help は <code>emulator -help</code> で確認できる。</p><hr/><p>timestamp<br/>
<!-- -->2017-07-17 (First edition)<br/>
<!-- -->2018-02-10 (Last modify)</p>]]></description>
      <pubDate>Mon Jul 17 2017 00:00:00 GMT+0000 (Coordinated Universal Time)</pubDate>
    </item>
    <item>
      <title>Android Things</title>
      <link>https://sukawasatoru.com/docs/android-things-raspberry-pi-3</link>
      <guid>https://sukawasatoru.com/docs/android-things-raspberry-pi-3</guid>
      <description><![CDATA[<h1>Android Things</h1><ul><li><a href="#install">Install</a><ul><li><a href="#%E4%BD%BF%E3%81%A3%E3%81%9F%E3%82%82%E3%81%AE">使ったもの</a></li><li><a href="#%E6%89%8B%E9%A0%86">手順</a></li></ul></li><li><a href="#ota">OTA</a></li></ul><h2>Install</h2><p>Raspberry Pi 3 に Android Things を install したときのメモ。<br/>
<a href="https://developer.android.com/things/hardware/raspberrypi.html">https://developer.android.com/things/hardware/raspberrypi.html</a> を上から読んでいけばその通りに設定できたので、とくに難しいことは無かった。リンク張ってあったり丁寧に書いてあったので困ることは無いはず。<br/>
<a href="https://partner.android.com/things/console">Console</a> で作成した Image は試していないです。</p><h3>使ったもの</h3><ul><li>Raspberry Pi 3 Model B</li><li>micro SD<ul><li>8GB 以上であれば何でもいいと思うけれど余っていた <a href="https://amazon.jp/dp/B008CTGX2U">これ</a> を使った</li></ul></li><li>USB - TTL Serial cable<ul><li>何でもいいと思うけれど <a href="https://amazon.jp/dp/B014CA59P4">これ</a> を使った</li></ul></li><li>HDMI cable</li><li>micro USB cable</li></ul><p>WiFi のネットワーク設定には Ethernet cable か Serial cable のどちらかが必要なようなので Serial を使用した</p><h3>手順</h3><p>Image を準備する</p><ol><li>Image のダウンロード<br/><a href="https://developer.android.com/things/preview/download.html">https://developer.android.com/things/preview/download.html</a></li><li>SD を FAT32 でフォーマットする</li><li>dd で SD に Image を書く<br/>コマンドはリンクが張ってあるのでそちらを参照すれば良い<br/><a href="https://www.raspberrypi.org/documentation/installation/installing-images/mac.md">https://www.raspberrypi.org/documentation/installation/installing-images/mac.md</a><br/>自分が使用したコマンドは <code>sudo dd bs=1m if=iot_rpi3.img of=/dev/rdisk2</code></li></ol><p>Android Things の起動、ネットワーク設定</p><ol><li>SD カードを Raspberry pi に挿入する</li><li>Serial cable を接続する<br/>端子のつなぎ方や通信の設定は <a href="https://developer.android.com/things/hardware/raspberrypi.html#serial_debug_console">https://developer.android.com/things/hardware/raspberrypi.html#serial_debug_console</a> の section に書いてある<br/>mac を使っているのでリンクが張ってあった Serial を App Store から買った</li><li>HDMI cable と 電源 (micro USB) を繋ぐ</li><li>しばらくすると boot animation が終わり Home っぽい画面に遷移する</li><li>Serial から以下を入力し WiFi を設定する<br/><code>am startservice -n com.google.wifisetup/.WifiSetupService -a WifiSetupService.Connect -e ssid &lt;SSID&gt; -e passphrase &lt;password&gt;</code></li><li>1分弱待つと Home に <code>&lt;SSID&gt;</code> や IP Address が表示される</li><li><code>adb connect &lt;IP Address&gt;</code> で connect 出来れば完了</li></ol><h2>OTA</h2><p>OTA を試みたときのメモ。<br/>
<!-- -->こちらも難しい点はとくになし。</p><p><a href="https://partner.android.com/things/console">Android Things Console</a></p><ol><li>Image の作成<br/>Console の FACTORY IMAGES タブから APK を含んだ image を作成する。<br/>APK を含まない image を作成すると Filename が <em>Empty bundle</em> となり、これを Raspberry Pi に書き込んでも OTA することができない。<br/>APK を upload するには <a href="https://developer.android.com/things/console/app_bundle.html">Create a Bundle <!-- -->|<!-- --> Android Things</a> に従った、 APK を含んだ zip を作成し、それを upload する。</li><li><em>CREATE BUILD CONFIGURATION</em> して image を作成する</li><li>作成した image を download して Raspberry Pi に書き込む</li><li>Raspberry Pi を通常起動する<br/>Create a Bundle の通りに作成した zip に含んだ APK が起動する。</li><li>OTA 用の image を作成する<br/>作成方法は 1 - 2 の通り。</li><li><em>OTA UPDATES</em> タブで OTA 用に作成した image を選択し <em>PUSH TO DEVICES</em> ボタンを押す<br/>計測はしていないが image の自動ダウンロードが始まるまで 5、6 時間掛かるようなことが help に書いてある。<br/>download が完了すると以下の log が出力される</li></ol><pre><code>07-30 00:49:43.035   169   169 I update_engine: [0730/004943:INFO:update_manager-inl.h(52)] ChromeOSPolicy::UpdateCheckAllowed: START
07-30 00:49:43.035   169   169 I update_engine: [0730/004943:INFO:chromeos_policy.cc(322)] Allowing update check.
07-30 00:49:43.036   169   169 I update_engine: [0730/004943:INFO:update_manager-inl.h(74)] ChromeOSPolicy::UpdateCheckAllowed: END
07-30 00:49:43.036   169   169 I update_engine: [0730/004943:INFO:update_attempter.cc(862)] Running periodic update.
07-30 00:49:43.036   169   169 I update_engine: [0730/004943:INFO:update_attempter.cc(205)] Last reported daily metrics 4h55m39.93465s ago.
07-30 00:49:43.036   169   169 I update_engine: [0730/004943:INFO:update_attempter.cc(272)] Not updating b/c we already updated and we&#x27;re waiting for reboot, we&#x27;ll ping Omaha instead
07-30 00:49:43.036   169   169 I update_engine: [0730/004943:INFO:metrics.cc(142)] Sending 4 for metric UpdateEngine.Check.Result (enum)
07-30 00:49:43.038   169   169 I update_engine: [0730/004943:INFO:metrics.cc(165)] Sending 4h55m39.933865s for metric UpdateEngine.Check.TimeSinceLastCheckMinutes
07-30 00:49:43.038   169   169 I update_engine: [0730/004943:INFO:metrics.cc(181)] Sending 4h55m39.932121s for metric UpdateEngine.Check.TimeSinceLastCheckUptimeMinutes
07-30 00:49:43.038   169   169 I update_engine: [0730/004943:INFO:action_processor.cc(46)] ActionProcessor: starting OmahaRequestAction
07-30 00:49:43.039   169   169 I update_engine: [0730/004943:INFO:action_processor.cc(116)] ActionProcessor: finished last action OmahaRequestAction with code ErrorCode::kSuccess
07-30 00:49:43.039   169   169 I update_engine: [0730/004943:INFO:update_manager-inl.h(52)] ChromeOSPolicy::UpdateCheckAllowed: START
07-30 00:49:43.039   169   169 I update_engine: [0730/004943:INFO:chromeos_policy.cc(316)] Periodic check interval not satisfied, blocking until 7/30/2017 5:50:23 GMT
07-30 00:49:43.039   169   169 I update_engine: [0730/004943:INFO:update_manager-inl.h(74)] ChromeOSPolicy::UpdateCheckAllowed: END
</code></pre><ol start="7"><li>Reboot する
download が完了したら Raspberry Pi を reboot する。<br/>再起動すると以下の log が出力される。<br/><em>OTA UPDATES</em> タブの Status にはすぐ反映されないが <code>dumpsys package &lt;package&gt;</code> で version を確認すると新しい version になっていることが確認できる。  </li></ol><pre><code>01-01 00:00:07.584   169   169 I update_engine: [0101/000007:INFO:main.cc(113)] Chrome OS Update Engine starting
01-01 00:00:07.633   169   169 I update_engine: [0101/000007:INFO:boot_control_android.cc(78)] Loaded boot_control HAL &#x27;AOSP reference bootctrl HAL&#x27; version 0.1 authored by &#x27;The Android Open Source Project&#x27;.
01-01 00:00:07.633   169   169 I update_engine: [0101/000007:INFO:real_system_state.cc(63)] Booted in dev mode.
01-01 00:00:07.633   169   169 W update_engine: [0101/000007:WARNING:real_system_state.cc(98)] No powerwash-safe directory, using non-volatile one.
01-01 00:00:07.666   169   169 I update_engine: [0101/000007:INFO:omaha_request_params.cc(66)] Initializing parameters for this update attempt
01-01 00:00:07.689   169   169 I update_engine: [0101/000007:INFO:prefs.cc(122)] img-prop-channel-name not present in /data/misc/update_engine/prefs
01-01 00:00:07.690   169   169 I update_engine: [0101/000007:INFO:prefs.cc(122)] img-prop-powerwash-allowed not present in /data/misc/update_engine/prefs
01-01 00:00:07.690   169   169 I update_engine: [0101/000007:INFO:omaha_request_params.cc(182)] Download channel for this attempt = stable-channel
01-01 00:00:07.690   169   169 I update_engine: [0101/000007:INFO:omaha_request_params.cc(77)] Running from channel stable-channel
01-01 00:00:07.694   169   169 I update_engine: [0101/000007:INFO:real_device_policy_provider.cc(167)] No device policies/settings present.
01-01 00:00:07.696   169   169 I update_engine: [0101/000007:INFO:payload_state.cc(864)] Current Response Signature =
01-01 00:00:07.696   169   169 I update_engine: Payload 0:
01-01 00:00:07.696   169   169 I update_engine:   Size = 1352490
01-01 00:00:07.696   169   169 I update_engine:   Sha256 Hash = 888a1225d41b28234d3756a690cadfe0205a2d4ce0381452e7aa4449beaee951
01-01 00:00:07.696   169   169 I update_engine:   Metadata Size = 918
01-01 00:00:07.696   169   169 I update_engine:   Metadata Signature = 
01-01 00:00:07.696   169   169 I update_engine:   Is Delta = 0
01-01 00:00:07.696   169   169 I update_engine:   NumURLs = 1
01-01 00:00:07.696   169   169 I update_engine:   Candidate Url0 = http://storage.googleapis.com/app-payloads-prod/2ndknh/2/full.payload
01-01 00:00:07.696   169   169 I update_engine: Payload 1:
01-01 00:00:07.696   169   169 I update_engine:   Size = 5829
01-01 00:00:07.696   169   169 I update_engine:   Sha256 Hash = 8c94194a4b391e5ca7386157ca3d1f1c2664a458587c45eed29a6122f823c56a
01-01 00:00:07.696   169   169 I update_engine:   Metadata Size = 133
01-01 00:00:07.696   169   169 I update_engine:   Metadata Signature = 
01-01 00:00:07.696   169   169 I update_engine:   Is Delta = 0
01-01 00:00:07.696   169   169 I update_engine:   NumURLs = 1
01-01 00:00:07.696   169   169 I update_engine:   Candidate Url0 = http://storage.googleapis.com/app-payloads-prod/2ndknh/2/vbmeta.STABLE_2.USERDEBUG.payload
01-01 00:00:07.696   169   169 I update_engine: Payload 2:
01-01 00:00:07.696   169   169 I update_engine:   Size = 221543
01-01 00:00:07.696   169   169 I update_engine:   Sha256 Hash = c96a2c54ed3bba50e36096a7afaaddc17ad03a54c6f8411cf9b6fd1746ee8d68
01-01 00:00:07.696   169   169 I update_engine:   Metadata Size = 165924
01-01 00:00:07.696   169   169 I update_engine:   Metadata Signature = 
01-01 00:00:07.696   169   169 I update_engine:   Is Delta = 1
01-01 00:00:07.696   169   169 I update_engine:   NumURLs = 1
01-01 00:00:07.696   169   169 I update_engine:   Candidate Url0 = http://storage.googleapis.com/os-payloads-prod/rpi3/0.0.0.4098522/userdebug_delta_0.0.0.4098522.payload
01-01 00:00:07.696   169   169 I update_engine: Max Failure Count Per Url = 10
01-01 00:00:07.696   169   169 I update_engine: Disable Payload Backoff = 0
01-01 00:00:07.696   169   169 I update_engine:
01-01 00:00:07.699   147   147 I SurfaceFlinger: SurfaceFlinger is starting
01-01 00:00:07.703   169   169 I update_engine: [0101/000007:INFO:payload_state.cc(880)] Payload Attempt Number = 3
01-01 00:00:07.710   169   169 I update_engine: [0101/000007:INFO:payload_state.cc(888)] Full Payload Attempt Number = 2
01-01 00:00:07.733   169   169 I update_engine: [0101/000007:INFO:payload_state.cc(914)] Current URL Index = 0
01-01 00:00:07.734   169   169 I update_engine: [0101/000007:INFO:payload_state.cc(545)] Current download source: Unknown
01-01 00:00:07.748   169   169 I update_engine: [0101/000007:INFO:payload_state.cc(958)] Current URL (Url0)&#x27;s Failure Count = 0
01-01 00:00:07.760   147   147 I SurfaceFlinger: SurfaceFlinger&#x27;s main thread ready to run. Initializing graphics H/W...
01-01 00:00:07.761   147   147 D libEGL  : Emulator forcing qemu.gles value to -1.
01-01 00:00:07.763   169   169 I update_engine: [0101/000007:INFO:payload_state.cc(947)] URL Switch Count = 0
01-01 00:00:07.786   169   169 E update_engine: [0101/000007:ERROR:payload_state.cc(974)] Invalid backoff expiry time (7/31/2017 5:19:44 GMT) in persisted state. Resetting.
01-01 00:00:07.786   169   169 I update_engine: [0101/000007:INFO:payload_state.cc(985)] Backoff Expiry Time = 4/22/2009 19:24:48 GMT
01-01 00:00:07.788   169   169 I update_engine: [0101/000007:INFO:payload_state.cc(1039)] Update Timestamp Start = 1/1/1970 0:00:07 GMT
01-01 00:00:07.788   169   169 I update_engine: [0101/000007:INFO:payload_state.cc(1114)] Update Duration Uptime = 0s
01-01 00:00:07.803   169   169 I update_engine: [0101/000007:INFO:payload_state.cc(1155)] Current bytes downloaded for HttpsServer = 0
01-01 00:00:07.816   169   169 I update_engine: [0101/000007:INFO:payload_state.cc(1180)] Total bytes downloaded for HttpsServer = 0
01-01 00:00:07.830   169   169 I update_engine: [0101/000007:INFO:payload_state.cc(1155)] Current bytes downloaded for HttpServer = 0
01-01 00:00:07.840   169   169 I update_engine: [0101/000007:INFO:payload_state.cc(1180)] Total bytes downloaded for HttpServer = 0
01-01 00:00:07.847   169   169 I update_engine: [0101/000007:INFO:payload_state.cc(1155)] Current bytes downloaded for HttpPeer = 0
01-01 00:00:07.862   169   169 I update_engine: [0101/000007:INFO:payload_state.cc(1180)] Total bytes downloaded for HttpPeer = 0
01-01 00:00:07.870   169   169 I update_engine: [0101/000007:INFO:payload_state.cc(763)] Number of Reboots during current update attempt = 0
01-01 00:00:07.881   169   169 I update_engine: [0101/000007:INFO:payload_state.cc(1192)] Num Responses Seen = 0
01-01 00:00:07.882   169   169 I update_engine: [0101/000007:INFO:prefs.cc(122)] rollback-version not present in /data/misc/update_engine/powerwash-safe/update_engine/prefs
01-01 00:00:07.891   169   169 I update_engine: [0101/000007:INFO:payload_state.cc(1379)] p2p First Attempt Timestamp = 4/22/2009 19:24:48 GMT
01-01 00:00:07.900   169   169 I update_engine: [0101/000007:INFO:payload_state.cc(1364)] p2p Num Attempts = 0
01-01 00:00:07.902   169   169 I update_engine: [0101/000007:INFO:update_manager-inl.h(52)] ChromeOSPolicy::UpdateCheckAllowed: START
01-01 00:00:07.904   169   169 I update_engine: [0101/000007:INFO:chromeos_policy.cc(316)] Periodic check interval not satisfied, blocking until 1/1/1970 0:11:15 GMT
01-01 00:00:07.904   169   169 I update_engine: [0101/000007:INFO:update_manager-inl.h(74)] ChromeOSPolicy::UpdateCheckAllowed: END
01-01 00:00:07.919   169   169 I update_engine: [0101/000007:INFO:prefs.cc(122)] attempt-in-progress not present in /data/misc/update_engine/prefs
01-01 00:00:07.920   169   169 E update_engine: [0101/000007:ERROR:payload_state.cc(1263)] time_to_reboot is negative - system_updated_at: 7/29/2017 9:54:11 GMT
01-01 00:00:07.924   169   169 I update_engine: [0101/000007:INFO:update_manager-inl.h(52)] ChromeOSPolicy::P2PEnabled: START
01-01 00:00:07.924   169   169 I update_engine: [0101/000007:INFO:update_manager-inl.h(74)] ChromeOSPolicy::P2PEnabled: END
01-01 00:00:07.924   169   169 I update_engine: [0101/000007:INFO:update_attempter.cc(1480)] Not starting p2p at startup since it&#x27;s not enabled.
01-01 00:00:07.924   169   169 I update_engine: [0101/000007:INFO:update_manager-inl.h(52)] ChromeOSPolicy::P2PEnabledChanged: START
01-01 00:00:07.924   169   169 I update_engine: [0101/000007:INFO:update_manager-inl.h(74)] ChromeOSPolicy::P2PEnabledChanged: END
</code></pre><p>refs.<br/>
<a href="https://developer.android.com/things/index.html">Android Things <!-- -->|<!-- --> Android Things</a><br/>
<a href="https://developer.android.com/things/hardware/raspberrypi.html">Raspberry Pi 3 <!-- -->|<!-- --> Android Things</a><br/>
<a href="https://developer.android.com/things/preview/download.html">System Image Downloads <!-- -->|<!-- --> Android Things</a><br/>
<a href="https://partner.android.com/things/console">Android Things Console</a><br/>
<a href="https://developer.android.com/things/console/app_bundle.html">Create a Bundle <!-- -->|<!-- --> Android Things</a></p><hr/><p>timestamp<br/>
<!-- -->2017-07-17 (First edition)<br/>
<!-- -->2020-02-07 (Last modify)</p>]]></description>
      <pubDate>Mon Jul 17 2017 00:00:00 GMT+0000 (Coordinated Universal Time)</pubDate>
    </item>
  </channel>
</rss>